# QA Report — 2026-09-16 — CUI-0025 / CUI-0028 / CUI-0032 batch（Round 2 驗收）

| 項目 | 內容 |
|---|---|
| 測試員 | Quality Assurance（獨立角色） |
| 日期 | 2026-09-16 |
| 類型 | **Batch QA**（3 個 fix，cap 5，合規） |
| 範圍 | CUI-0025（🟢，`points` contract，**有前端 breaking change**）、CUI-0028（test gap）、CUI-0032（dev extra） |
| 環境 | `/home/user/Python-Project-Run365Days` 主 checkout，branch `claude/ai-dev-team-start-05jie2`，HEAD `02d5846` |
| 上游 | Batch review `2026-09-16_review_CUI-0025_batch.md`（88/100 ⚠️ warn）→ Delta review `..._delta.md`（**90/100 ✅ pass**） |
| 前置 | Reviewer 兩輪已核實：五個決定嘅 mutation teeth、S-058/059/060 數字、S-053 兩條互咬 test、CUI-0032 Vercel 三層前提、W-029 三個裁決。**本報告唔重複**，只做行為驗證、回歸、同 reviewer 冇覆蓋嘅角度 |
| 總結 | ✅ **pass** — 0 🔴 Critical / 0 🟡 blocking。三張票**全部建議標 completed**。新開 **CUI-0033 – CUI-0036** 四張 follow-up，全部唔阻結案 |

---

## 1. Hard Gates

全部喺主 checkout 實跑，非引述。

| Gate | 指令 | 結果 |
|---|---|---|
| Tests（Python） | `.venv/bin/python -m pytest tests -q` | ✅ **pass** — **402 passed** in 50.66 s |
| Lint（Python） | `.venv/bin/ruff check src tests` | ✅ **pass** — All checks passed! |
| Format（Python） | `.venv/bin/ruff format --check src tests` | ✅ **pass** — 58 files already formatted |
| SDL 同步 | `.venv/bin/run365-schema --check frontend/schema.graphql` | ✅ **pass** — is up to date |
| Lint（前端） | `npm run lint` | ✅ **pass** — exit 0，零 output |
| Type check | `npm run typecheck` | ✅ **pass** — codegen SUCCESS + `tsc -b` exit 0 |
| Tests（前端） | **`npm test`**（= `npm run codegen && vitest run`）| ✅ **pass** — **131 passed / 22 files** |
| Coverage | `pytest tests -q --cov=src --cov-report=term-missing` | ✅ **pass** — **1632 stmts / 82 miss / 95% TOTAL** |
| Build（api mode） | `npm run build` | ✅ **pass** — built in 2.64 s |
| Build（static mode） | `npm run build:static` | ✅ **pass** — built in 2.53 s；bundle 內含新嘅 `points must be between 1 and …` 檢查 |
| Data integrity | 真實 export 重跑 + 逐 byte 比對 | ✅ **pass** — 見 §4.1 |
| Performance | 請求預算逐個邊界實測 | ✅ **pass** — 見 §4.2 |
| No Critical | 本報告 | ✅ **pass** — **0 🔴** |
| Security | `pip-audit`（reviewer）+ **`npm audit`（本輪首次）** | ⚠️ **pass with advisory** — 見 §1.1 |

**Hard gates 13 pass + 1 pass-with-advisory。402 / 131 / 95% 三個數同 delta review 逐字相同。**

### 1.1 Security — `npm audit` 從來冇人行過（→ CUI-0036）

Reviewer 喺 CUI-0032 行咗 `pip-audit`（Python 側零漏洞）。**冇任何一輪行過 `npm audit`。**
本輪行咗：**14 vulnerabilities（2 moderate、12 high）**。

- 12 high：`@graphql-codegen/*` 鏈（根源 `lodash` `_.template` code injection）
- 2 moderate：`vitest` / `@vitest/mocker`（path traversal）

**核實過暴露面：14 個全部只經 `devDependencies` 到達。** `frontend/package.json` 嘅八個
`dependencies` 一個都唔喺名單；`grep -rn "graphql-codegen\|lodash" frontend/src`（排除測試）**零命中**。
所以 **零 browser / 零 runtime 暴露**，暴露面係 build-time 同 CI。

而且 `git diff --stat 6cb7529..HEAD -- pyproject.toml frontend/package*.json` **為空** ——
**唔係本 batch 引入**。因此判 **pass with advisory，唔阻三張票結案**，
另開 **CUI-0036**（process gap：兩個 audit 都唔喺 `.github/workflows/pages.yml`）。

---

## 2. Section A — CUI-0025（風險最集中：前端 breaking change）

### 2.1 主動測試方法（reviewer 冇做嘅角度）

Reviewer 用 mutation testing 驗「每個決定有牙」。QA 換一個軸：**用真嘢行真路徑**。

| 用嘅嘢 | 具體 |
|---|---|
| 真 static source | `createStaticSource`（唔係手砌 object），真 fetcher 讀真 JSON |
| 真 api mode | `create_app()` + `POST /api/graphql`（真 Flask + 真 Strawberry，行真 DB） |
| 真數據 | 365 條真 activity，由 `run365-export` 重新產生落 scratch |
| 真 UI | `ActivityView` 喺 jsdom 入面真 render，行 `useTrack` → TanStack Query → `readableError` → `TrackError` |
| 真「之前」 | `git show 72dfcb5^:frontend/src/data/static/source.ts`，喺 scratch copy 上跑同一組 case |

> 🔒 全部喺 scratchpad 嘅 copy 上做。主 repo 嘅 `src/` / `tests/` / `frontend/src/` 由頭到尾冇被寫過（§7）。

### 2.2 合法值：同「之前」逐點一致 ✅

`git show 72dfcb5^` 嘅舊 `track()`（`return points ? downsample(rows, points) : rows;`）
對真 export 行同一組 case：

| `points` | 改動前 | 改動後 | 逐點一致？ |
|---|---|---|---|
| `1` | 1 點 `[5628]` | 1 點 `[5628]` | ✅ |
| `600` | 600 點 | 600 點 | ✅ |
| `1000` | 600 點 | 600 點 | ✅ |
| omitted | 600 點（全部）| 600 點（全部）| ✅ |

**四個合法輸入，輸出陣列逐個元素相等。CUI-0025 對現有 caller 零行為改變。**

非法值（即個 breaking change 本身）：

| `points` | 改動前 | 改動後 |
|---|---|---|
| `0` | 靜靜回**全部 600 點**（falsy 短路 —— AU-001 堵過嘅 fan-out）| ❌ `RangeError` |
| `1001` | 靜靜回 600 點（冇上界）| ❌ `RangeError` |
| `2.5` | 靜靜回 **2 點** `[0, 3880]` | ❌ `RangeError` |

> 📌 `2.5` → 2 點呢個「靜靜錯」係實測到嘅，唔係推論。票上第五個決定（非整數一併拒絕）**證實有實據**。

### 2.3 新拋嘅 `RangeError` 真係有人接 ✅，而且訊息唔係 blob ✅

喺 scratch copy 用 jsdom 真 render `ActivityView`（真 `createStaticSource` + 真 `DataProvider`）：

```
RENDERED >>> Could not load the track for this activity: points must be between 1 and 1000, got 0Retry
RETRY    >>> track() calls before/after: 1 → 2
```

- ✅ `ActivityView.tsx:164` 個 `TrackError` 真係接得住（reviewer 個講法**核實成立**）
- ✅ 上面嘅 header / weather / KPI 區照樣 render（CUI-0016 嘅局部降級仍然成立）
- ✅ Retry 掣真係重新叫 `track()`
- ✅ **用戶睇到嘅全句 93 字元**，其中 error message 本身 **40 字元**

> **明確回答「會唔會又係一個 601 字元 blob（CUI-0030）」：唔會。**
> CUI-0030 嗰個 blob 嚟自 `graphql-request` 嘅 `ClientError.message`（序列化成個 response + request）。
> 呢個新 error 係 client 側自己掟嘅 `RangeError`，`readableError()` 行到第三個 fallback
> （`error.response` 唔存在 → 直接用 `error.message`），出嚟就係嗰一句。
> 三個非法值嘅長度：`0` → 40 字元、`1001` → 43、`2.5` → 42。
> **而且 `ActivityView` 已經係四個 view 入面唯一一個用咗 `readableError()` 嗰個**，
> 所以 CUI-0030 嘅未修範圍同呢條新路徑冇交集。

🟢 **一個附帶觀察（唔開票）**：production `QueryClient` 係 `retry: 1`（`DataProvider.tsx:25`）。
`RangeError` 係 deterministic，retry 一定再失敗。今日成本為零（bounds check 喺 fetch 之前，
一個 request 都唔會發），而且 `retry: 1` 係 pre-existing、對所有 error class 一視同仁，
所以唔值得為 CUI-0025 單開票。記錄喺此。

### 2.4 兩個 mode 嘅 contract 真係對齊咗？——逐字、逐點核實

真 Flask + GraphQL vs 真 `createStaticSource`，同一個 activity（`7264441638`）：

| `points` | api mode | static mode | 拒絕與否 | 訊息逐字 | 輸出逐點 |
|---|---|---|---|---|---|
| `0` | ❌ `points must be between 1 and 1000, got 0` | ❌ 同一句 | ✅ 一致 | ✅ **逐字相同** | — |
| `1` | ✅ 1 點 | ✅ 1 點 | ✅ | — | ✅ **相同** |
| `600` | ✅ 600 點 | ✅ 600 點 | ✅ | — | ✅ **相同** |
| `1000` | ✅ 600 點 | ✅ 600 點 | ✅ | — | ✅ **相同** |
| `1001` | ❌ `... got 1001` | ❌ 同一句 | ✅ 一致 | ✅ **逐字相同** | — |
| `-1` | ❌ `... got -1` | ❌ 同一句 | ✅ 一致 | ✅ **逐字相同** | — |
| `2.5` | ❌ `Int cannot represent non-integer value: 2.5` | ❌ `points must be between 1 and 1000, got 2.5` | ✅ 一致 | ❌ **唔同句** | — |
| `2147483648` | ❌ `Int cannot represent non 32-bit signed integer value: …` | ❌ `points must be between 1 and 1000, got 2147483648` | ✅ 一致 | ❌ **唔同句** | — |
| omitted | 150 點（SDL default）／600 點（`createApiSource`） | 全部 | ❌ **唔一致** | — | ❌ |

**結論：CUI-0025 個核心承諾（「拒絕同一批值」）100% 成立；
「講同一句」喺 GraphQL `Int!` 表達得到嘅範圍內 100% 成立**，
`Int!` 表達唔到嗰兩個值（非整數、超 Int32）兩邊都拒絕但句子唔同。
`checkPoints` docstring 有披露非整數嗰個，冇披露超 Int32 嗰個 → **CUI-0033 (b)**，🟢 純 docstring。

另外核實咗 `track()` 註釋嘅一句 claim：**「Checked before the fetch, so an illegal `points` costs no request」成立** ——
`track("no-such-activity", 0)` 出 `RangeError` 而唔係 `ENOENT`，即係真係一個 request 都冇發 ✅。
（同一個叫法喺 api mode 回 `{"activity": null}` 零 error → **CUI-0033 (c)**，🟢。）

### 2.5 `undefined` 分支：S-061 核實 —— **claim 成立，而且仲深一層**（→ CUI-0033 (a)）

Reviewer 開 S-061 講：`undefined` 嘅「bounded by construction」靠 `DEFAULT_POINT_LIMIT`（600）
同 `DEFAULT_TRACK_POINTS`（600）啱啱相等，行 `run365-export --points 1200` 分歧會原樣返嚟。

**QA 真係跑咗一次 `--points 1200`**（輸出去 scratch，`data/processed/` 冇被寫過，§7 有核數）：

| 量度 | `--points 600`（default） | `--points 1200` |
|---|---|---|
| 超過 600 行嘅 track | 0 / 365 | **2 / 365** |
| 最長（activity `7264441638`） | 600 行 | **1200 行** |

跟住兩個 mode 各打一次：

| 叫法 | static mode | api mode |
|---|---|---|
| `track(id, 1200)` | ❌ `RangeError: … got 1200` | ❌ 同一句 |
| `track(id, 1000)` | ✅ 1000 點 | ✅ 1000 點（**逐點相同**）|
| **`track(id)`（omitted）** | ⚠️ **1200 點** | **600 點** |

**S-061 成立 ✅。** 而且量到第二層 reviewer 冇提過嘅嘢：

> 同一個 function **拒絕**「畀我 1200 點」，但**照回** 1200 點畀一個冇講數量嘅 caller。
> `MAX_TRACK_POINTS` 係「你可以問幾多」嘅上限，**唔係**「你會收到幾多」嘅上限。

→ **CUI-0033 (a)**，🟡。QA 傾向方案 C（omitted 保持「回全部」但用 `MAX_TRACK_POINTS` 封頂），
因為佢尊重用戶「omitted 回全部」嘅決定，今日行為零改變，又唔係第二個 breaking change。

### 2.6 兩種語言嘅 `MAX_TRACK_POINTS` —— S-062 收緊（→ CUI-0034）

Reviewer 話「冇任何嘢令佢哋唔同步時紅」。QA 實測之後要**收緊呢句**：

- 改 **TS** 常數（1000 → 999）→ **3 條測試紅** ✅（`source.test.ts:73/78/86`）
- 但釘住佢哋嘅係**三個寫死字面量** `"points must be between 1 and 1000, got …"`，同 Python **零關係**
- 所以改 **Python** 常數 → `run365-schema --check` 紅 → 更新 SDL → gate 綠返 → **TS 側全綠、兩邊分歧**

**個 gap 係單向嘅**：Python → SDL 有 gate，SDL → TS 冇。
呢個正正係本 batch review 自己喺 **S-065** 標出嚟嘅形態（釘字面量而唔係釘來源），同一輪出現兩次。
→ **CUI-0034**，🟢（reviewer §11 已明文建議另開 CUI 票）。

### 2.7 Section A 判定

| 項目 | 結果 |
|---|---|
| 五個決定行為上成立 | ✅ 全部 |
| 合法值同改動前逐點一致 | ✅ |
| 新 error 有人接、訊息可讀、Retry 可用 | ✅ |
| 兩 mode 拒絕/接受決定一致 | ✅ 九個值全中 |
| 兩 mode 合法輸出逐點一致 | ✅ |
| Production 現存 call site 受影響？ | ❌ 零 —— 唯一 call site `ActivityView.tsx:34` 傳 `TRACK_POINTS = 600`（合法），`useTrack` 係唯一 hook |
| Edge case gap | 3 個 🟢/🟡 follow-up（CUI-0033 a/b/c、CUI-0034），**全部唔阻結案** |

**Verdict：✅ pass，建議標 completed。**

---

## 3. Section B — CUI-0028（test gap）

Reviewer 已驗過三條新測試嘅 teeth（`"0"` / `""` mutation 都紅）。
QA 驗**佢哋描述嘅 invariant 喺真實 export 上成唔成立**。

### 3.1 「today's production export 靠固定寬度令 key injective」—— ✅ 成立

`src/api/service.py` `SAMPLE_KEY_SEPARATOR` docstring 嘅聲稱，對 `data/processed/run365.db` 實測：

| 聲稱 | 量度 | 結果 |
|---|---|---|
| id 全部純十進制 | 365 個 id | ✅ 全部 `isdecimal()` |
| id 固定寬度 | 寬度分佈 | ✅ `{10: 365}` —— 一個寬度 |
| 固定寬度令 separator 唔承重 | 用 `":"` / `"0"` / `""` / `"5"` 四個 separator 重砌全部 row key | ✅ **四個都係 0 個 cross-activity 碰撞** |

即係 docstring 講嘅「Every other fixture, and today's production export, carries either a
non-decimal prefix or a fixed width, and either one makes the key injective on its own」
**逐字成立**，亦解釋咗點解個 suite 綠咗咁耐。

### 3.2 `test_the_sample_key_separator_cannot_occur_in_a_position` 嘅 invariant —— ✅ 喺真數據上成立

條測試喺 `COLLIDING_TRACK_LENGTHS + VARIED_TRACK_LENGTHS` 上 assert。
QA 拎**真 export 嘅 123 個 distinct track length（250–600）** × 6 個 `points` 值（1/2/7/150/600/1000）
重跑同一個 assertion：**0 個 position 含有 separator，0 個非 int**。✅

條測試本身亦**唔係 vacuous**：佢自己 assert 掃出嚟嘅 position 湊齊 `set("0123456789")`，
即係話 separator 就算被改成一個罕見數字都走唔甩。設計質素高。

### 3.3 DoD 五項

| # | DoD | 核實 |
|---|---|---|
| 1 | 測試喺 separator 變數字/空字串時會紅 | ✅ reviewer 已 mutation 驗；QA 核實條 assertion 非 vacuous |
| 2 | 覆蓋變長 id batch | ✅ `COLLIDING_IDS = ("1","01","5","05","50","500")`，寬度 1–3，含 leading zero |
| 3 | 兩個 mutation 都紅 | ✅ reviewer 已驗（唔重複） |
| 4 | docstring 指返測試名 | ✅ `src/api/service.py:216-227` 兩條測試名齊 |
| 5 | pytest 全綠 + ruff clean | ✅ 402 passed，ruff 兩項 clean |

**Verdict：✅ pass，五項全剔，建議標 completed。**

---

## 4. Section C — CUI-0032（dev extra）+ 回歸

### 4.1 乾淨 venv：直接行到 ✅

```
python3 -m venv <scratch>/cleanvenv
<scratch>/cleanvenv/bin/pip install -e ".[dev]"     → OK（pytest-cov 7.1.0, coverage 7.16.1）
<scratch>/cleanvenv/bin/python -m pytest tests -q --cov      → 402 passed, 1632 / 82 / 95%
<scratch>/cleanvenv/bin/python -m pytest tests -q --cov=src  → 402 passed, 1632 / 82 / 95%
```

- ✅ 新 venv `pip install -e ".[dev]"` 之後 `pytest --cov=src` **直接行到**，唔使手動裝嘢
- ✅ `[tool.coverage.run] source = ["src"]` 嘅設計目標達成：**bare `--cov` 同 `--cov=src` 係同一個 run**（逐字相同）
- ✅ **冇令任何已記錄嘅 coverage 數字改變**：1632 / 82 / 95% 同 `.proj-docs/` 已記錄嘅逐字相同
- ✅ 依賴 delta 為空（見 §1.1），license：`pytest-cov` MIT（票上已核實）

### 4.2 S-066 影響核實 —— 比 reviewer 講嘅嚴重一級（→ CUI-0035）

Reviewer 講 `source = ["src"]` 令 `api/graphql.py` 永遠唔入分母，評「零現存風險」。
**QA 量咗被隱起嗰半究竟係乜：**

| | statements | miss | cover |
|---|---|---|---|
| `api/graphql.py` | 44 | **23** | **48%** |
| TOTAL（`--cov=src --cov=api`） | 1676 | 105 | **94%** |
| TOTAL（今日記錄嘅 `--cov=src`） | 1632 | 82 | **95%** |

冇覆蓋嗰 23 句係 `45-52, 57-78, 89-90`：

- `57-78` = **`_error_app()` 成個 function**
- `89-90` = `_build_app()` 個 `except`，唯一通往 `_error_app` 嘅路
- `45-52` = `_load_package_from_source()` 個 fallback body

即係：**一個整份都係為咗「deploy 爛咗嗰陣仲睇得到點解爛」而存在嘅功能，
本身冇一條測試行過，而 config 保證咗冇一份 coverage report 會提起佢。**
（`tests/test_api.py:47` 個 `VERCEL_ENTRY` 確實真正 `exec_module`，所以嗰 48% 係真執行覆蓋，
唔係靜態掃描 —— 但走嘅全部係 happy path。）

**唔阻 CUI-0032 結案**（本票之前根本冇 `[tool.coverage.run]`，唔係 regression），
但 S-066 方案 A（`source = ["src", "api"]`）**應該先補測試再改分母**，
否則就係為咗一個 48% 嘅數去改一堆歷史紀錄。→ **CUI-0035**，🟡。

**Verdict：✅ pass，建議標 completed。**

---

## 5. 回歸測試

### 5.1 Export byte-identical ✅

重跑 `run365-export`（輸出落 scratch），同 `data/processed/` 逐 byte 比：

| 產出 | 結果 |
|---|---|
| `static/**.json`（370 個檔）| ✅ **369 個 byte-identical**；唯一差異係 `meta.json` |
| `meta.json` | ✅ 只差 `generated_at` 時間戳，其餘逐字相同 |
| `run365.db` | ✅ `sqlite3.iterdump()` **136,058 行，得 1 行唔同**，就係 `meta` 表嘅 `generated_at` |

**Export pipeline 完全 deterministic，本 batch 零影響。**

### 5.2 CUI-0027 budget 收費 —— 全部照收 ✅

真 Flask + GraphQL 逐個邊界試（真 export DB）：

| 情境 | 結果 |
|---|---|
| 4 × `weather(limit:1000)` = 4000 rows | ✅ OK |
| 5 × `weather(limit:1000)` = 5000 rows | ❌ `list row budget exhausted: one request may read at most 4000 rows` |
| 2 × weather + 2 × weight（各 1000） | ✅ OK —— **兩種 list field 共用同一個 budget** |
| 2 × weather + 3 × weight（各 1000） | ❌ 同上拒絕 |
| `weather(limit:1001)` | ❌ `limit must be between 1 and 1000, got 1001`（`MAX_PAGE_SIZE`）|
| `activities(limit:64){ track(points:1) }` | ✅ OK — 64 條 track |
| `activities(limit:65){ track(points:1) }` | ❌ `track field budget exhausted: … at most 64 tracks` |
| `activities(limit:64){ track(points:156) }`（= 9,984） | ✅ OK |
| `activities(limit:64){ track(points:157) }`（= 10,048） | ❌ `track points budget exhausted: … at most 10000 track points` |
| 10 × `track(points:1000)` = 10,000 | ✅ OK（邊界 inclusive）|
| 11 × `track(points:1000)` = 11,000 | ❌ 拒絕 |
| 60 個 alias field 嘅 document | ❌ `Document contains more than 1000 tokens`（`MAX_QUERY_TOKENS` 先咬）|

**`MAX_LIST_ROWS_PER_REQUEST` 嘅收費一個都冇被整壞**，
而且 `64 × 156 / 64 × 157` 呢對正正重現咗 `schema.py` docstring 自己寫嘅 worked example。✅

### 5.3 CUI-0021 兩個 mode 取樣對齊 ✅（本輪最大規模嘅比對）

真 Flask + GraphQL（分批，每批都過晒 token / field / points budget）vs 真 `createStaticSource`，
**全部 365 條 activity × 3 個 `points` 值（150 / 600 / 1000）**：

```
cross-mode comparisons: 1,095（activity × points）
point-for-point mismatches: 0
total sec values compared: 322,832
```

**零差異。** CUI-0021 嘅取樣對齊 + CUI-0025 嘅範圍對齊，喺真數據全量比對之下完全成立。

### 5.4 CUI-0019 batch predicate ✅

40 條真 activity 一次 `service.tracks(session, ids, 150)`，攔截 SQL：

```
SQL statements issued: 2
  SELECT ... count(*) ... WHERE track_points.activity_id IN (?, ?, …)   IN-lists: 1 | OR arms: 0
  SELECT anon_1.activity_id, anon_1.seq, ...                            IN-lists: 2 | OR arms: 0
```

**兩句 statement、零 `OR` arm、單一 `IN`。** AU-050 嗰種 per-track disjunction 冇返嚟。✅
（40 條 track × 150 = 6,000 行，全部正確返回。）

### 5.5 CUI-0006 `parse_datetime` ✅

- 針對性測試：`pytest -k "parse_datetime or datetime"` → **35 passed**
- 真數據：`data/processed/activities_{tcx,gpx}.jsonl` 嘅 **730 個真時間戳**全部 parse 得，0 failure
- 抽樣：`2021-01-01 07:10:55` → `2021-01-01T07:10:55+08:00`（`Asia/Hong_Kong`）✅
- 最強嘅一重證據其實係 §5.1：export 重跑 byte-identical，而 export 嘅每一條 activity 都經過 `parse_datetime`

---

## 6. 跨 fix 互動評估（batch 模式強制）

| 檢查 | 結果 |
|---|---|
| 三個 fix 有冇改同一個 file 嘅唔同 function | CUI-0025 掂 `src/api/schema.py` + `frontend/src/data/static/source.ts`；CUI-0028 掂 `tests/test_api.py` + `src/api/service.py`（只加 docstring）；CUI-0032 掂 `pyproject.toml`。**`src/api/` 之下 schema.py 同 service.py 分別由兩個 fix 掂到** —— 但 CUI-0028 嗰邊純 docstring，零行為交集 ✅ |
| 新 helper / 新常數 symbol collision | `MAX_TRACK_POINTS`（TS，新 export）唔同 `src/api/schema.py` 同名常數喺同一個 namespace；`checkPoints` 係 module-private。零 collision ✅ |
| 三個 fix 合埋會唔會撞 GAS 知識條目 | 逐條掃過 CLAUDE.md §6 七條陷阱：`PYTHONTZPATH` / TDD Red / `inf` / NOT NULL / Vercel 偵測 / Vercel 依賴 / `tzdata` / `MissingTimeZoneDataError` —— 三個 fix 冇一個掂到嗰七個位。**但 CUI-0032 同「Vercel 唔裝 optional extras」呢條有邏輯關係**：`pytest-cov` 入咗 `[dev]` 而唔係 core，方向啱，reviewer 已三層核實 ✅ |
| `pyproject.toml` 同時被 CUI-0032（coverage）同既有 ruff / packaging 設定共用 | `ruff check` / `ruff format --check` / `pytest` 三個都綠，`[tool.coverage.run]` 冇影響到 `[tool.ruff]` 或 `[tool.pytest.ini_options]` ✅ |
| Batch 整體 coverage threshold | 1632 stmts / 95%，同 batch 前逐字相同 ✅ |
| Batch 整體改動規模 | 三個 fix + 兩輪 review 修訂，行為改動只集中喺一個 function（`checkPoints` / `track()`）✅ |

**跨 fix 互動：零問題。**

---

## 7. 驗證環境聲明（強制）

- 所有量度喺 `/home/user/Python-Project-Run365Days` 主 checkout，或者 scratchpad 內嘅 **copy** 上進行。
- 兩個 export 實驗（default 同 `--points 1200`）**全部用 `--db` / `--static-dir` 寫去 scratchpad**，
  `data/processed/` 由頭到尾冇被 export 寫過。收工逐檔核數：

  ```
  374 個檔案 sha256 逐個比對 baseline → IDENTICAL
  ```

- 「改動前」嘅 `source.ts`、「TS 常數 mutate」嘅實驗，全部喺 scratchpad 嘅 `frontend/` copy 上做
  （`node_modules` 用 symlink），主 repo 嘅 `frontend/src/` 一個字都冇被寫過。
- UI render 測試檔寫入 scratchpad copy，跑完即刪，**冇寫入過 repo**。
- `npm run build` / `npm run build:static` 產生嘅 `frontend/dist/` 已刪除（本身亦 git-ignored）。
- 收工核查：

  ```
  $ git status --porcelain     → (只有本報告同四個新 ticket)
  $ git diff --stat            → (空)
  $ git diff --cached --stat   → (空)
  ```

  ✅ **零 production code 改動、零測試改動、零臨時遺留。**
- 本 agent **冇**執行任何 `git merge` / `git push` / `git commit` / `git mv`。

---

## 8. 問題清單同優先順序

| # | 嚴重度 | 問題 | Ticket | 阻結案？ |
|---|---|---|---|---|
| 1 | 🟡 Medium | static `track(id)`（omitted）唔受 `MAX_TRACK_POINTS` 管；`--points 1200` 之下回 1200 點，而同一 function 會拒絕「畀我 1200 點」。兩 mode 分歧（1200 vs 600）原樣返嚟 | **CUI-0033 (a)** | ❌ 唔阻 |
| 2 | 🟢 Low | 非整數 / 超 Int32 兩邊都拒絕但**句子唔同**；docstring「same sentence」冇講範圍 | **CUI-0033 (b)** | ❌ 唔阻 |
| 3 | 🟢 Low | 未知 id + 非法 `points`：static 掟 `RangeError`，api 靜靜回 `null` | **CUI-0033 (c)** | ❌ 唔阻 |
| 4 | 🟢 Low | `MAX_TRACK_POINTS` 兩個獨立寫死 1000；TS 側被**字面量**釘住，Python → TS 方向冇 gate | **CUI-0034** | ❌ 唔阻 |
| 5 | 🟡 Medium | Vercel 入口嘅 start-up 失敗路徑（`_error_app`，23/44 句）零測試，而且被 `source=["src"]` 永久隱形 | **CUI-0035** | ❌ 唔阻（pre-existing） |
| 6 | 🟡 Medium | `npm audit` 從來冇喺任何 gate 行過；今日 14 個漏洞（12 high），全部 dev 鏈、零 runtime 暴露 | **CUI-0036** | ❌ 唔阻（pre-existing，零依賴 delta） |

**🔴 Critical：0。**

---

## 9. 三張票嘅狀態建議（QA 獨立判斷）

| Ticket | 建議 | 理由 |
|---|---|---|
| **CUI-0025** | ✅ **completed** | 五個決定行為上逐個成立；合法值同改動前**逐點一致**；breaking change 嘅新 error 真係有人接、訊息 40 字元可讀、Retry 可用；兩 mode 九個值嘅拒絕決定全中、合法輸出 1,095 組零差異；production 唯一 call site 傳合法值。四個 follow-up 全部係**新範圍**（`--points` 入口、Int32 邊界、Python→TS gate），唔係本票未做完 |
| **CUI-0028** | ✅ **completed** | DoD 五項齊（第 4 項 S-067 已補）；reviewer 已驗 teeth，QA 另外核實咗佢哋描述嘅 invariant 喺**真實 export** 上成立（365 個 id 全部 10 位純數字，四個 separator 零碰撞），而且條 invariant test **非 vacuous** |
| **CUI-0032** | ✅ **completed** | 乾淨 venv `pip install -e ".[dev]"` → `pytest --cov` 直接行到；bare `--cov` 同 `--cov=src` 逐字同一個 run；**零已記錄 coverage 數字改變**（1632/82/95%）。S-066 嘅影響已量化並轉入 CUI-0035，屬新範圍 |

> ⚠️ **提醒 main agent**：reviewer 嘅 S-061 – S-066 六個 Suggestion 之中，
> **S-061 → CUI-0033**、**S-062 → CUI-0034**、**S-066 → CUI-0035** 已由本輪接手成票。
> 餘下 **S-063 / S-064 / S-065** 同新開嘅 **S-068 – S-071** 仍然 open，由 main agent 決定幾時做。

---

## 10. 測試建議（下一步）

1. **先做 CUI-0034 方案 A**（由 SDL parse 返個界）。佢成本最低而且會令 CUI-0033 (a) 嘅任何修法
   自動被 gate 住 —— 次序上應該行喺 CUI-0033 前面。
2. **CUI-0033 (a) 要用戶拍板**：方案 B 係第二個 breaking change，方案 C（omitted 封頂 1000）
   今日行為零改變。QA 傾向 C，但「omitted 回全部」係用戶明文決定，唔應該由 developer 自行改讀法。
3. **CUI-0036 次序要緊**：先入 CI gate 再 bump，否則下一輪一樣會靜靜哋累積。
   ⚠️ 加 gate 會即刻令 CI 紅（今日已有 12 high），所以兩件事要同一個 PR 上。
4. **系統性觀察**：本 batch 兩次撞到同一個形態 —— **斷言釘住字面量而唔係釘住來源**
   （S-065 嘅 `str(MAX_LIST_ROWS_PER_REQUEST)`、CUI-0034 嘅三條 `"…1 and 1000…"`）。
   值得喺 review checklist 加一項：「呢條斷言入面嘅數字，係邊度嚟？改咗來源會唔會紅？」

---

## 11. Handoff receipt

```handoff-receipt
protocol: 2
agent: quality-assurance
status: pass
critical: 0
report: .proj-docs/qa/2026-09-16_qa_CUI-0025_batch.md
tickets_pass: CUI-0025, CUI-0028, CUI-0032
tickets_blocked: none
new_tickets: CUI-0033, CUI-0034, CUI-0035, CUI-0036
next_action: invoke_devops
notes: 三張票行為驗證全部通過，1,095 組跨 mode 比對零差異、export byte-identical；四張新票全部係新範圍 follow-up，唔阻 release。
```
