# Batch QA — 2026-09-17 — 14 張票（CUI-0037/0038/0039/0040/0005/0012/0013/0008/0041/0035/0036/0042/0018(a)/0018(b)）

> **📁 檔案有冇建立：冇。** 我冇試寫任何 `.md`（brief 叫唔好嘥時間）。以下係完整報告原文，
> 可以原文寫入 `.proj-docs/qa/2026-09-17_qa_batch.md`。

**HEAD 我見到嘅第一行：`2ff659e Merge pull request #10 from dcwhung/chore/claude/ai-dev-team-plugin`**
—— 即係第 **十二** 次撞同一個陳舊 base。已 `git fetch origin claude/ai-dev-team-start-05jie2`
再 checkout，現時 HEAD = **`71dad80 docs: file CUI-0046 and refresh the index`**。全部量度都喺 `71dad80` 度做。

**我冇做過**：任何 commit / merge / push / 改任何 project 代碼 / dispatch 任何 workflow。
臨時 mutation 共 2 次（`src/api/schema.py`），做完即刻還原並 md5 核實
（`778820fba74bf26b315f1b7d13128398`，兩次都對得返）。收工時 `git status --short` **空**，
`frontend/package.json` + `package-lock.json` md5 亦核實過無變。臨時檔寫喺 worktree 內
`.qa-tmp/` 同 `frontend/qa-tmp*/`（scratchpad 俾 project-boundary hook 擋咗），已全部刪除。

---

## 整體 verdict

| 項目 | 結果 |
|---|---|
| Hard gates | **6/6 pass** |
| 🔴 Critical | **0** |
| 14 張票 | **14 張全部可以標 `completed`**（CUI-0036 有一個 bookkeeping 條件，見下） |
| 新開 ticket | **5 張**（CUI-0047 … CUI-0051），全部 🟢 Low / 🟡 Medium，**冇一張 block release** |
| 推翻上游 | **3 處**（1 個 ticket 數字、1 個 test comment 數字、1 個量度方法陷阱） |
| Status | ✅ **pass** |

### Hard Gates

| Gate | 指令 | 結果 |
|---|---|---|
| Tests (py) | `.venv/bin/python -m pytest tests -q --cov` | ✅ **529 passed**，147.58s |
| Tests (fe) | `cd frontend && npm test` | ✅ **25 files / 143 tests passed** |
| Coverage | 同上 | ✅ **TOTAL 96%**（1789 stmts / 63 missed）|
| Lint | `ruff check` / `ruff format --check` / `npm run lint` | ✅ 全 0（`60 files already formatted`）|
| Type check | `npm run typecheck`（先跑 codegen）| ✅ exit 0 |
| SDL sync | `run365-schema --check frontend/schema.graphql` | ✅ `is up to date` |
| Builds | `npm run build` + `npm run build:static` | ✅ 兩個都 exit 0 |
| Security | `npm audit --omit=dev` | ✅ `found 0 vulnerabilities` |
| Data integrity | 真 `run365.db`（365 activities / 357 GPS / **134,041** track rows）端到端 | ✅ |

**brief 俾嘅基準我逐個獨立量返，全部對**：529 pytest ✅、143 vitest / 25 files ✅、TOTAL 96% ✅、
`api/schema.py` 341/0 **100%** ✅、`api/service.py` 112/0 **100%** ✅、`api/graphql.py` 44/0 **100%** ✅。
`src/cli/collect_weather.py` 45/1 **98%**，唯一未覆蓋嗰行係第 150 行 `main()`（`if __name__` guard，唔可覆蓋）。
`pyproject.toml` 註釋話「加 `api` 唔會郁 TOTAL 百分比」—— 算術核實成立：
含 api = 63/1789 → 96.48%，`--cov=src` = 63/1745 → 96.39%，兩個都 round 到 96%。

---

## Section A — CUI-0037 / CUI-0038：log 洩漏面（端到端）

**方法**：用 `importlib` 由路徑 `exec_module()` **真 `api/graphql.py`**（唔係 copy）、真 `run365.db`、
真 WSGI server（`werkzeug.make_server`，唔係 `test_client`）、**OS 層 `os.dup2` 捕捉 fd 2**，
乜 logging 都唔設（Vercel 預設）。斷言過 `logging.getLogger().handlers == []` 同
`strawberry.execution` 冇 handler、effective level = WARNING 先開始量。

> ⚠️ **量度陷阱（我第一版量錯，記低）**：werkzeug 嘅 `_internal._log` 第一次 log 時會**自己** `addHandler`
> 一個 StreamHandler 落 `werkzeug` logger，於是每個 request 寫 **72 bytes** access log 落 fd 2。
> 呢 72 bytes 係 harness artifact（Vercel 冇 werkzeug access log），唔封住就會把「0 bytes」量成「72 bytes」。
> 第二版用 `request_handler=Quiet` + 清 `werkzeug` handler 先量到真數。

### 應該 0 bytes 嗰批 —— 全部 **0**

| Scenario | stderr bytes | tb frames | `extensions` keys | `data` |
|---|---|---|---|---|
| `{ activities(offset:-1) { id } }` | **0** | 0 | `["code"]` ARGUMENT_OUT_OF_RANGE | `null` |
| `{ activities(limit:1001) { id } }` | **0** | 0 | `["code"]` ARGUMENT_OUT_OF_RANGE | `null` |
| `track(points:0)` | **0** | 0 | `["code"]` ARGUMENT_OUT_OF_RANGE | **唔係 null** |
| `track(points:1001)` | **0** | 0 | `["code"]` ARGUMENT_OUT_OF_RANGE | **唔係 null** |
| budget: TRACK_FIELD（65×1pt）| **0** | 0 | `["code"]` TRACK_FIELD_BUDGET_EXCEEDED | 唔係 null |
| budget: TRACK_POINTS（11×1000pt）| **0** | 0 | `["code"]` TRACK_POINTS_BUDGET_EXCEEDED | 唔係 null |
| budget: LIST_ROW（5×1000 rows）| **0** | 0 | `["code"]` LIST_ROW_BUDGET_EXCEEDED | `null` |
| control：一個正常服務嘅 request | **0** | 0 | — | 唔係 null |

### 必須保留嗰三條 —— 全部仍然響

| Scenario | stderr bytes | tb frames | 備註 |
|---|---|---|---|
| resolver fault | **2343** | **9** | ✅ marker 命中 |
| `Int` coercion `2.5` | **179** | **0** | 同 docstring「已經零 frame」一致 |
| `Int` coercion `2147483648` | **207** | **0** | 同上 |
| unseeded budget `RuntimeError` | **2776** | **10** | ERROR + traceback，冇被 reclassify |

**獨立 oracle**：我喺 `service.meta` 種咗 `QA-ORACLE-7f3a91c4-RESOLVER-FAULT`。
`marker_in_stderr` 只喺 resolver-fault 嗰格 `True`，其餘 11 格全部 `False` ——
證實我捉到嗰 9 個 frame 真係我製造嗰個，唔係撞彩收到人哋嘅 traceback。

### CUI-0038 / W-030：雙邊斷言真係兩邊都食力（in-process mutant + oracle）

按 `CLAUDE.md` §6 嘅方法：in-process `setattr` → **讀返個值確認 mutant 真係生效** → 用獨立 oracle
（fd-2 bytes ＋ `LogRecord.levelno`，兩個都唔係被測嗰句 assert）判定。

| `REFUSAL_LOG_LEVEL` | mutant 生效? | bounds bytes | budget bytes | record levelno | `INFO <= x < WARNING` |
|---|---|---|---|---|---|
| **INFO（出廠）** | ✅ | **0** | **0** | 20 | **True** |
| DEBUG（W-030 mutant）| ✅ | **0** | **0** | 10 | **False** ← 只有**下界**殺到 |
| WARNING（CUI-0038 mutant）| ✅ | **102** | **332** | 30 | False ← 只有**上界**殺到 |
| ERROR（CUI-0029 mutant）| ✅ | **102** | **332** | 40 | False |

`logging.lastResort.level == 30 (WARNING)` ✅，所以「WARNING 就係 configureless 門檻」呢個前提成立。
**DEBUG 嗰行係關鍵證據**：DEBUG 一樣 0 bytes，即係 security property 完全保住，
`< logging.WARNING` 那半**捉唔到佢** —— 只有 W-030 加嘅 `logging.INFO <=` 殺得死。雙邊斷言名副其實。

> 🔴 **推翻上游 #1（數字）**：`tests/test_api.py` 嗰段註釋寫
> 「at WARNING one bounds refusal writes **103** bytes to stderr and one budget refusal **341**」。
> 我用 `{ activities(offset:-1) { id } }` 同 5×`activities(limit:1000)` 量到 **102** 同 **332**；
> 用 `track(points:0)` 同 65-activity list 量到 **161** 同 **239**。
> 差異唔係矛盾 —— **係因為註釋冇講佢用邊個 document**，而 byte 數完全由 document 決定。
> 呢個正正係 S-068 / S-069 已經喺其他地方立咗規矩要修嘅同一種病（「講明喺邊個檔量」）。
> → 開 **CUI-0051**（🟢 docs）。

### Verdict
- **CUI-0037 ✅ 可標 `completed`** —— 四條 bounds 路實測 0 bytes / 0 frame，`BoundsError` 繼承
  `ClientRefusalError` 生效。
- **CUI-0038 ✅ 可標 `completed`** —— 雙邊 bound 經 4-point mutant sweep 證實兩邊都 load-bearing。
- **Edge case 補測**：✅ 無 gap（我加咗 configureless 端到端 + DEBUG/WARNING/ERROR 四點 sweep，
  現有測試用 `caplog` 已足夠，唔建議再加 —— `caplog` 會裝 handler，反而量唔到 configureless 真相）。

---

## Section B — CUI-0018 (a)+(b)：breaking change（最重要）

### (b) nullability

SDL 實測：`track(points: Int! = 150): [TrackPoint!]` ✅（list nullable、element non-null）
`activities(...): [Activity!]!` ✅ **冇跟住 widen**。

**一個被拒 `track` 只 null 一個 field**，raw body 原文：
```
{"data":{"meta":{"year":2021,"generatedAt":"2026-09-16T11:08:55"},
 "activity":{"id":"7264441638","distanceKm":11.22,"track":null}},
 "errors":[{"message":"points must be between 1 and 1000, got 0",
 "locations":[{"line":1,"column":71}],"path":["activity","track"],
 "extensions":{"code":"ARGUMENT_OUT_OF_RANGE"}}]}
```
兄弟 field `meta.year` / `meta.generatedAt` / `activity.id` / `activity.distanceKm` 全部仲喺度 ✅

**一個被拒 page 仍然清空成個 response** ✅：`activities(offset:-1)` / `activities(limit:1001)` /
LIST_ROW budget → `data: null`。

### 四個 code 嘅邊界（真 export，365 activities）

| document | code | 第一個 null 嘅 index | 服務到幾多 | data null? |
|---|---|---|---|---|
| `activities(limit:64) … track(points:1)` | *(無 error)* | — | 64 | 否 |
| `activities(limit:65) … track(points:1)` | **TRACK_FIELD_BUDGET_EXCEEDED** | 64 | 64 | 否 |
| `activities(limit:10) … track(points:1000)` | *(無 error)* | — | 10 | 否 |
| `activities(limit:11) … track(points:1000)` | **TRACK_POINTS_BUDGET_EXCEEDED** | 10 | 10 | 否 |
| `activities(limit:64) … track(points:156)` (=9984) | *(無 error)* | — | 64 | 否 |
| `activities(limit:64) … track(points:157)` (=10048) | **TRACK_POINTS_BUDGET_EXCEEDED** | 63 | 63 | 否 |
| `activities(limit:65) … track(points:1000)` | **TRACK_POINTS**（55 條）| 10 | 10 | 否 |

兩個 code 喺邊界上分得清清楚楚。最後一行順帶實測證實 docstring 講嘅
「**points budget 唔黐底**（not sticky）」：65 個 field × 1000 點出 **55 = 65−10** 條獨立 POINTS 拒絕，
而唔係一次 latch —— 因為 field counter 喺 points 失敗時冇被扣（`fields_left` 停喺 54），
所以第 65 個 field 都係行 POINTS 而唔係 FIELD。

> ⚠️ **推翻上游 #3（量度方法，我自己踩咗先寫低）**：想用 **65 個 aliased `activity(id:)` root**
> 去觸發 field budget 係**做唔到**嘅 —— 個 document 有 3100+ 字元，
> 先撞 `MaxTokensLimiter`（`Syntax Error: Document contains more than 1000 tokens`），
> 而**嗰條 error 唔喺 `ClientRefusalError` 家族，會照寫 3633 bytes 落 stderr**。
> Field budget **只能經一條 list field** 到達（`activities(limit:65, hasGps:true)`，約 25 個 token）。
> 任何人再量呢個 budget 要用 list 形式，唔好用 alias fan-out。

### `extensions` 淨係得 `code`（讀 raw response text，唔經 parsed body）

我喺**每一個** scenario 嘅原始 response 字串度搵以下 9 個名，**全部 absent everywhere**：
`track_points_remaining`、`track_fields_remaining`、`list_rows_remaining`，
加 camelCase 同 `run365_*` 三組變體。W-013 條線守住 ✅
另外：fault / coercion 嘅 `extensions` 係**空**（`ext_keys: []`），
即 `test_a_server_fault_carries_no_client_refusal_code` 嗰條線亦成立。

### static ↔ api 行為一致（四個 case）

| `points` | static mode | api mode | 一致? |
|---|---|---|---|
| `0` | reject `points must be between 1 and 1000, got 0`，**0 次 fetch** | reject 同一句，code `ARGUMENT_OUT_OF_RANGE`，`track: null` | ✅ 決定一致、**句子逐字一樣** |
| `>1000` (1001) | reject 同一句 | reject 同一句 | ✅ |
| 非整數 (2.5) | reject `points must be between 1 and 1000, got 2.5` | reject `Int cannot represent non-integer value: 2.5`，**`data: null`** | ⚠️ 決定一致、**措辭同影響面唔同**（已寫死喺 `static/source.ts` 個表入面）|
| omitted | resolve，**成條 stored track**（上限 1000）| resolve，**150 行**（`DEFAULT_TRACK_POINTS`）| ⚠️ **行數唔同**，但兩邊都唔會 reject 亦唔會回 null |

`-1` 同 `2147483648` 我一併驗埋：static 兩個都 reject；api `-1` 出 `ARGUMENT_OUT_OF_RANGE`，
`2147483648` 出 `Int cannot represent non 32-bit signed integer value`。
CUI-0033 (c) 嗰個「unknown id」分歧亦重現：static `track("NOPE",10)` throw，
api `{ activity: null }` **零 error**；但 `activity(id:"NOPE") { track(points:2.5) }` 兩邊都 reject（coercion 先行）。

**兩個 build mode 都行過**：`npm run build` exit 0、`npm run build:static` exit 0。

### 🔎 我補嘅 edge case（呢批冇人寫過，**而且係整個改動嘅命根**）

`frontend/src/data/api/source.ts` 同 `types.ts` 嘅註釋話
「a refusal always arrives with an entry in `errors`, and `graphql-request` rejects on that first,
so this line is not reached」—— **呢個前提全批冇一條測試守住**。
如果 `graphql-request` 唔 reject，`activity?.track ?? []` 就會**靜靜哋畫一個空 chart**
而唔係出 `TrackError`。我對住 `node_modules` 入面嘅真 `graphql-request` 寫咗 4 條，**全綠**：

1. partial `data` + `errors`（被拒 track）→ **reject** ✅
2. bounds 拒絕 → **reject** ✅
3. unknown id（完全冇 `errors`）→ **resolve `[]`** ✅（另一種 null，正確地被吞）
4. `extensions.code` 真係去到 thrown error 上（`err.response.errors[0].extensions.code`）✅

→ 呢 4 條係我建議 developer 落地嘅補測（見最尾「建議補測」）。

### Verdict
- **CUI-0018 (a) ✅ 可標 `completed`**
- **CUI-0018 (b) ✅ 可標 `completed`**
- 殘留（**已知並刻意 defer，非缺陷**）：四條 list field 嘅 `limit` / `offset` 分唔開，見 Section G。

---

## Section C — CUI-0012：靜默錯先係重點

### `warnings.py` marker `-1` —— 我自己重現咗，**lane 講得啱**

用舊 code（`html[html.find(marker)+len(marker):]`）逐格量：

| 輸入 | `find(marker)` | slice offset | 舊 code parse 到 |
|---|---|---|---|
| `hko_warning_day.html`（原有 fixture，有 marker）| 270 | 302 | **3 條正確記錄** |
| **原有 fixture 但我手術剷走 marker** | **−1** | **31** | **仍然係同樣 3 條正確記錄** ← **證明唔到缺陷** |
| `hko_warning_day_no_marker.html`（新 fixture）| **−1** | **31** | **1 條錯嘅**：`('ROW FROM AN UNRELATED TABLE', '1999-07-05 03:00:00', '1999-07-06 04:00:00')` |

機制我亦量實：offset 31 跳過嘅 31 個字元係 `'<html>\n  <body>\n    <table>\n   '`，
即係**啱啱斬斷咗第一個 `<table>` 開 tag**，令 `find_all("table")` 搵唔到嗰個孤兒 `<tr>`，
所以 `SIGNAL BEFORE THE MARKER` 嗰行根本入唔到結果。**新 fixture 真係必要，唔係多餘。**
新 code 對兩個 fixture：有 marker → 945 字元 section；冇 marker → `WeatherPageStructureError`。✅

### `hourly.py` icon code `-1` 算術

| script | 舊 slice | 舊描述 | 新 `_icon_code` | 新描述 |
|---|---|---|---|---|
| `drawWeatherIcon(7, 'CurrentWeather', 'x')`（真 shape）| `'7'` | Rain | `'7'` | Rain |
| **`drawIcon(7)`（票講嗰個）** | **`'7'`** | **Rain** ← 票講得啱 | `None` | Unknown |
| `console.log('hello')` | `"onsole.log('hello'"` | Unknown | `None` | Unknown |
| `xx, 'CurrentWeather'`（只有 suffix）| `'x'` | Unknown | `None` | Unknown |
| **`n(26`（只有 prefix）** | **`'2'`** | **Few clouds** ← 票**冇**提過嘅第二宗 | `None` | Unknown |
| `""` | `''` | Unknown | `None` | Unknown |
| 完全冇 `<script>` | `AttributeError` **成日 crash** | — | `None` | Unknown |

`drawIcon(7)` 靠 `end == -1` 令 slice 變 `script[9:-1]`；`n(26` 靠 `end == -1` 令 slice 變 `script[2:-1] = '2'`
—— **兩個都係 `-1` 做 slice index，出兩個唔同嘅錯描述**。票只講咗第一宗，第二宗我補上。

### 我補嘅 edge case：「冇單位嘅 cell 唔好蝕咗尾幾個字」呢個承諾

| wind cell | OLD | NEW |
|---|---|---|
| `'Northeast 50° 24 Km/h'` | 24.0 | 24.0 |
| `'Variable at 20 Km/h'` | 20.0 | 20.0 |
| **`'Northeast 50° 24'`（冇單位）** | **None** | **24.0** ✅ |
| **`'Variable at 20'`** | **None** | **20.0** ✅ |
| `'Northeast 50°'` | None | None |
| `'-'` / `''` | None | None |

| temperature cell | OLD | NEW |
|---|---|---|
| `'11 °C'` | 11.0 | 11.0 |
| **`'11'`（冇單位）** | **None** | **11.0** ✅ |
| `'-9 °C'` | −9.0 | −9.0 |
| `'5°C'` | 5.0 | 5.0 |

`_parsing` helper 我另外壓咗一批冇人送過嘅輸入：
`section_after(html, "")` → **靜靜哋回成個 html**（`find("")==0`，唔會 raise —— 今日只有常數 marker 入到，
低風險但值得知）；`section_after("M","M")` → `""`；`child_attr(cell,'img','class')`（multi-valued）→ `''`
（正如註釋所講）；`child_attr(None,…)` → `''`；`child_string(None,…)` → `None`。
`WeatherPageStructureError` 實測 `issubclass(..., RuntimeError) == True`、
`issubclass(..., ValueError) == False` ✅ —— 即 docstring 講嗰個「唔好俾 `except ValueError` 食咗」成立。

### Verdict：**CUI-0012 ✅ 可標 `completed`**

---

## Section D — CUI-0013：weather CLI

- Coverage 實測 `src/cli/collect_weather.py` **45 stmts / 1 missed / 98%**，唯一 miss 係第 150 行
  `main()`（`if __name__ == "__main__"` guard）。票講 45%→98% ✅
- `_COLLECTION_FAILURES = (requests.RequestException, WeatherPageStructureError)`，per-source 隔離 ✅
- `_REQUEST_TIMEOUT` 收歸 `common/config.py`：我用 **identity** 驗（唔係 equality，跟 S-090 條線）——
  `hko_daily` / `hourly` / `warnings` 三個 module 嘅 `HTTP_REQUEST_TIMEOUT`
  **`is config.HTTP_REQUEST_TIMEOUT` 全部 `True`**，值 `(5, 30)` ✅。三個 module 都冇再自己定義 `_REQUEST_TIMEOUT`。
- 失敗改 `logger.warning(..., exc_info=True)` ✅

**Verdict：CUI-0013 ✅ 可標 `completed`**

---

## Section E — CUI-0008 / CUI-0041

### CUI-0008 haversine
`(nan,0)` / `(inf,0)` / destination 帶 `-inf` → 全部 `ValueError: non-finite coordinate: …` ✅
`(None, inf)` → 回 `nan` **唔 raise**（`None` check 排喺前面）—— 設計上「indoor 冇 fix」優先，合理。
`(-0.0,-0.0)` → 0.0 ✅。`("22.3","114.1")` → `TypeError`（改動前後一樣，唔算 regression）。

🔎 **我補到嘅 gap（新，開 CUI-0047）**：guard 只 close 咗「非有限」，**冇 close「唔係一個座標」**。
`lat=400` → 4447.8 km、`lat=91` → 10118.7 km、`lat=lon=1e20` → 5560.1 km ——
全部**理直氣壯咁回一個數**，而 `parse_finite_float("1e20")` 照收（佢真係 finite）。
再推到 `lat=lon=1e308` → **`nan`** + `RuntimeWarning: invalid value encountered in sin`
（numpy argument reduction 頂唔住），而 `pandas.Series([1.0, nan, 2.0]).sum() == 3.0` ——
**即係 CUI-0001 / CUI-0008 立項要消滅嗰個「nan 靜靜哋跌出總和、距離報細咗」嘅形狀，
由一道 guard 冇 cover 嘅門返咗嚟**。嚴重性 🟢 Low（要 1e308 級數先出 nan；
但 `lat=400` 呢類「合理咁錯」嘅值一早已經冇人查）。
**同 CUI-0043 唔重複** —— CUI-0043 講 TCX 座標俾 `optional_float` 洗成 `None`（guard 射唔中），
我呢個講 guard **射得中但射唔穿**。

### CUI-0041 finite_float
`hasattr(numeric, "finite_float") == False` ✅，`numeric` 公開名淨返 `['finite','math','round_or_none','to_float']`。
⚠️ **提防**：`grep -rn finite_float src tests api frontend/src` 有 **13 個 hit**，
但逐個睇：**11 個係 `parse_finite_float`（`activities/parsers/base.py` 一個完全唔同嘅 function，仍然在用）**，
另 2 個係 `numeric.py` / `test_weather_models.py` 嘅解釋性 prose。**真 caller = 0** ✅
（單睇 grep 數字好易誤判成「刪唔清」，所以寫低。）

**Verdict：CUI-0008 ✅ / CUI-0041 ✅，兩張都可標 `completed`**

---

## Section F — CUI-0040 service.tracks

`points` 掃描（對住真 export 嘅 `7264441638`，stored 600 行）：

| `points` | 結果 |
|---|---|
| `-1` | ✅ `ValueError: points must not be negative, got -1` |
| `-0.5` | ✅ `ValueError: … got -0.5` |
| **`-0.0`** | ⚠️ **600 行**（唔 raise，而且 falsy ⇒ 當「冇問過」）|
| `0` / `None` / `False` | 600 行（= 成條 track，符合設計）|
| `True` / `1` | 1 行 |
| `1000` / `10**9` | 600 行（cap 生效）|
| `2.5` | ⚠️ `TypeError: 'float' object cannot be interpreted as an integer`（由 sampler 深處掟，docstring 只寫 `ValueError`）|
| **空 id list + `-1`** | ✅ **仍然 raise** —— guard 排喺 early-return 前面，**呢條冇人測過** |

🔎 **開 CUI-0048**（🟢 Low）收 `-0.0` 同 `2.5` 兩粒。兩粒都**到唔到 client**
（`_track_points` 同 `checkPoints` 都先 refuse，`Int` coercion 更加先 refuse），
所以係 public function 嘅契約整潔問題，唔係可利用缺陷。

**Verdict：CUI-0040 ✅ 可標 `completed`**

---

## Section G — CUI-0005：production 行為不變（真 WSGI server）

**唔用 request 數做 gate**（brief 講得啱：134 / 100 / 39 三個數係 GC 嘅性質）。
改驗**機制**：每個開咗嘅 session 有冇閂。identity 用**單調 serial** 而唔係 `id()`
（我第一版用 `id()`，因為 CPython 會重用 id，量出個假嘅「39 distinct」—— 已修正）。

| transport | lifecycle | opened | closed | **leaked** | 40 個 request 服務到 |
|---|---|---|---|---|---|
| 真 WSGI server | **fixed（teardown_request）** | 40 | 40 | **0** | 40 |
| `test_client` | **fixed** | 40 | 40 | **0** | 40 |
| 真 WSGI server | **pre-fix（call_on_close）** | 40 | 40 | **0** | 40 ← **production 由頭到尾冇壞過** |
| `test_client` | **pre-fix** | 40 | **0** | **40** | 40 |

`src/api/app.py` 註釋講「a real WSGI server opened 40 and closed 40, the test client opened 40 and closed none」
—— **逐個數字重現** ✅

加測兩條（冇人寫過）：
- **health endpoint**：5 個 `/api/health` → opened **0** / closed **0**，`teardown` 乜都 pop 唔到 ✅
  （即係 `g.pop(SESSION_KEY, ())` 個 default 真係要）
- **resolver 掟嘢嘅 request**：5 個 faulting request → opened **5** / closed **5** / leaked **0** ✅
  —— 呢個先係 `teardown_request` 相對 `call_on_close` 嘅真着數（`call_on_close` 喺 response
  冇正常 iterate 完嘅情況唔一定行）

> 📌 順帶一提：pre-fix + `test_client` 我頭先跑過一次真係爆
> `sqlalchemy.exc.TimeoutError: QueuePool limit of size 5 overflow 10 reached`，
> 第二次(改咗計數方式後)同樣 40 個 request **冇爆**（洩漏咗 40 個但 GC 收返晒 connection）。
> **同一個 build、同一日、兩次唔同結果** —— 直接印證「唔好把 request 數當 gate」。

**Verdict：CUI-0005 ✅ 可標 `completed`**

---

## Section H — CUI-0035 / CUI-0036 / CUI-0042（CI 同部署入口）

### CUI-0035
- **`api/graphql.py` 一行未改**：`git diff --stat 5f55e36..71dad80 -- api/` **完全空** ✅
  md5 `8a35fbe62e9606a04b224233095628bb`。
- `app = _build_app()` 仍然係最底一行 **top-level 無縮排** statement ✅（`CLAUDE.md` §6 條鐵律守住）
- `pyproject.toml` **只**改咗 `[tool.coverage.run] source = ["src"] → ["src","api"]` 加註釋，
  逐行核過 diff，冇掂過 `[project]` / `dependencies` / `[tool.ruff]` / `[tool.pytest]` ✅
- 測試真係 `exec_module()` 出貨嗰個檔（`_load_vercel_entry`），唔係 copy；`api/graphql.py` coverage **100%** ✅
- **Verdict：✅ 可標 `completed`**

### CUI-0036
- Gate 綠：`npm audit --omit=dev` → `found 0 vulnerabilities`，exit **0** ✅
- 全樹紅：`npm audit` → **14 vulnerabilities (2 moderate, 12 high)**，exit **1**
  —— `pages.yml` 個註釋寫「14 advisories — 12 high, 2 moderate, 0 critical」**逐個數字對** ✅
- **紅綠真係分得開**（brief 要求）：我喺 scratch copy（`.qa-tmp/auditred/`，**冇 commit**）
  加 `lodash@4.17.11` 落 `dependencies`、`npm install --package-lock-only` 重生 lock、
  再行**同一條 CI 指令** → **exit 1**，報 `1 critical severity vulnerability`。
  做完 `md5sum -c` 核實真 `frontend/package.json` + `package-lock.json` **完全無變**。
- Step 排位：`Audit runtime dependencies` 係 `frontend` job **最後一步**（S-097）✅
- ⚠️ **Bookkeeping 條件**：張票狀態寫「✅ done（gate 部分；bump 另行處理，見下）」，
  而 `pages.yml` 個註釋寫 dev 樹嗰 14 條「are tracked separately in **CUI-0036**」。
  **即係一搬去 `completed/`，嗰 14 條 advisory 就冇咗歸屬。** CUI-0044 講嘅係 Python 側 `pip-audit`，
  唔 cover 呢個。→ 開 **CUI-0049** 承接，然後 CUI-0036 先好搬。
- **Verdict：✅ 可標 `completed`，但要先開 CUI-0049 接手 dev-tree bump**

### CUI-0042 —— **我冇 dispatch 任何 workflow**（明文）
純靜態：`yaml.safe_load` 解 parse + 把 `Validate tag name` 個 `run:` body **原文抽出**，
用 `bash -e -c` 配 stub `git` / `gh`（放喺臨時 PATH）真行。

Step gating 真值表（由 parsed YAML 算，唔係讀 prose）：

| `release` | `notes_only` | tag | extract | create | edit |
|---|---|---|---|---|---|
| T | F | **YES** | YES | **YES** | no |
| F | F | **YES** | no | no | no |
| T | **T** | **no** | YES | no | **YES** |
| F | **T** | **no** | YES | no | **YES** |

→ `notes_only=true` 時 `release` **真係被完全忽略**（兩行一模一樣）✅
→ `create` 同 `edit` **永遠唔會同時 YES** ⇒ 冇 `create || edit` fallback ✅（註釋講嘅刻意分開步驟成立）

`Validate tag name` body 實跑 9 個 case：

| TAG | notes_only | tag 存在 | Release 存在 | exit | 第一行輸出 |
|---|---|---|---|---|---|
| `v1.2.3` | false | 冇 | — | **0** | *(靜)* |
| `v1.2.3` | false | **有** | 有 | **1** | `Tag v1.2.3 already exists on origin` |
| `1.2.3` | false | 冇 | — | **1** | `Tag must look like v1.2.3, got '1.2.3'` |
| `v1.2` | false | 冇 | — | **1** | 同上 |
| `v1.2.3-rc1` | false | 冇 | — | **1** | 同上（pre-release suffix 唔收）|
| `v1.2.3` | **true** | **冇** | — | **1** | `notes_only asked to correct v1.2.3, but no such tag exists on origin` |
| `v1.2.3` | **true** | 有 | **冇** | **1** | `notes_only asked to correct the notes of v1.2.3, but no Release is published for it` ← **S-093 條線** |
| `v1.2.3` | **true** | 有 | 有 | **0** | `notes_only: v1.2.3 exists and has a published Release; the tag will not be touched` |
| `nope` | true | 有 | 有 | **1** | `Tag must look like v1.2.3, got 'nope'`（格式檢查行喺 mode 分流之前）|

- **Verdict：CUI-0042 ✅ 可標 `completed`**

---

## Section I — CUI-0039

`test_a_refusal_and_a_fault_in_one_operation_are_logged_apart` 我用 mutation 驗佢真係唯一守門人：
把 `super().process_errors(faults, …)` 改成 `super().process_errors(errors, …)`
（file mutation + 清 `__pycache__`，做完 md5 還原核實），跑 `tests/test_api.py`：

```
1 failed, 227 passed
FAILED tests/test_api.py::test_a_refusal_and_a_fault_in_one_operation_are_logged_apart
```

**死咗剛剛好一條，就係 CUI-0039 嗰條。** 即係冇呢條測試，呢個 mutant 227 條測試之下會完全生存
—— 票講「the one operation shape that can see the error filter」**實測成立** ✅

**Verdict：CUI-0039 ✅ 可標 `completed`**

---

## 跨 fix 互動評估

| 檢查 | 結果 |
|---|---|
| `src/api/schema.py` 被 3 條 lane 改（`b73510e` CUI-0037 / `3ee16c3`+`7895f78` CUI-0018 / `08e2d84` W-035 cleanup）| ✅ AST 掃描：**59 個 module-level 定義，0 個重複名** |
| 四個 refusal code 有冇撞 | ✅ 四個值全部唔同（`ARGUMENT_OUT_OF_RANGE` / `LIST_ROW_BUDGET_EXCEEDED` / `TRACK_FIELD_BUDGET_EXCEEDED` / `TRACK_POINTS_BUDGET_EXCEEDED`）|
| `tests/test_api.py` 被 **12 個 commit**（5 條 lane）改 | ✅ `def test_` 名 **0 重複**；228 條全部 collect + pass |
| 新 helper / constant symbol collision | ✅ `_parsing.py` 三個 helper 名 + `HTTP_*_TIMEOUT` 三個 constant 全新，`grep` 無同名 |
| conflict marker 殘留 | ✅ 無（掃到嘅 `=====` 係 `schema.py` docstring 入面嘅 reST 表格框線）|
| 兩條 lane 改同一 file 嘅 import 順序 | ✅ `ruff check`（含 isort `I001`）全綠，`known-third-party = ["graphql"]` 仍在 |
| coverage threshold | ✅ 三個 api 檔仍然 100%，TOTAL 96% 冇跌 |
| GAS-001/002/003 知識條目 | ⬜ **呢個 repo 冇呢啲條目**（`grep -rn "GAS-00"` 只中 2026-09-14 嗰份 QA report 自己講「無此條目」）。無法評估。|

---

## 推翻上游 / 更正

| # | 對象 | 上游講 | 我量到 |
|---|---|---|---|
| 1 | `tests/test_api.py` CUI-0038 註釋 | WARNING 之下 bounds **103** bytes、budget **341** bytes | **102** / **332**（`activities(offset:-1)` + 5×`activities(limit:1000)`），或 **161** / **239**（`track(points:0)` + 65-activity list）。**註釋冇講用邊個 document**，所以無法重現 → CUI-0051 |
| 2 | `.tickets/pending/…/CUI-0045.md` | `tests/test_api.py` **3,166 行 / 197 個測試** | **3,543 行 / 228 個 collected test**。票嘅數字係舊嘅 —— **結論唔變，反而更強** |
| 3 | 量度方法（我自己先踩，寫低俾下手）| —（冇人講過）| 65 個 aliased `activity(id:)` **到唔到 field budget**，先撞 `MaxTokensLimiter`，而嗰條 error **唔係 `ClientRefusalError`，會照寫 3633 bytes 落 stderr**。Field budget 只能經 list field 到達 |

**其餘全部經得起實測**：CUI-0012「原 fixture 證明唔到」（逐格重現）、
`app.py`「40 開 40 閂 vs 40 開 0 閂」（逐個數字重現）、
`CLAUDE.md` 新 trap「type-only 倒返轉頭紅 **5** 條」（**實測剛剛好 5 條**，
mutant 生效由 SDL 變 `[TrackPoint!]!` 做獨立 oracle 確認）、
CUI-0039「唯一睇得到 filter 嗰條」（mutant 殺 1/228）、
S-093「check Release 唔淨係 tag」、`pages.yml`「14 條 / 12 high / 2 moderate / 0 critical / 全 dev-only」。

---

## 新開 Ticket（現有最高 **CUI-0046**，已掃 `.tickets/` 同 `.proj-docs/tickets.md` 確認；由 **CUI-0047** 起，無撞號）

> 我冇建立 ticket 檔案（隔離 worktree 入面建咗 main agent 都見唔到）。以下係完整內容，請 main agent 歸檔。

### CUI-0047 🟢 Low —— `haversine_distance` 守到「非有限」守唔到「唔係座標」
- **類型**：bug / robustness ｜ **負責人**：Backend Developer
- **來源**：QA 2026-09-17（CUI-0008 主動測試）｜ **相關**：CUI-0008、CUI-0001、CUI-0043
- **問題**：CUI-0008 加嘅 guard 只 reject `±inf` / `nan`。實測 `lat=400` → 4447.8 km、
  `lat=91` → 10118.7 km、`lat=lon=1e20` → 5560.1 km，全部**回一個似模似樣嘅數**；
  `lat=lon=1e308` → **`nan`** + `RuntimeWarning: invalid value encountered in sin`。
  `parse_finite_float("1e308")` 照收（真係 finite），而 `pandas.Series.sum()` 會靜靜哋跌走 `nan`
  —— **即 CUI-0001 / CUI-0008 立項要消滅嗰個「距離報細咗」形狀，由 guard 冇 cover 嘅門返咗嚟**。
- **建議**：喺 guard 加 `-90 <= lat <= 90` / `-180 <= lon <= 180`，掟同一個 `ValueError`
  （`parse_all` 已有 `ValueError` 分支，唔使動 layering）。
- **驗收**：`hav((400,0),(0,0))` / `hav((0,200),(0,0))` / `hav((1e308,1e308),(-1e308,-1e308))` 三個都 raise；
  現有 357 條 GPS activity 嘅 export 結果逐 byte 不變。

### CUI-0048 🟢 Low —— `service.tracks` 個 negative guard 唔 close 於 `-0.0`，`2.5` 掟未記錄嘅 `TypeError`
- **類型**：bug / contract ｜ **負責人**：Backend Developer ｜ **相關**：CUI-0040、CUI-0033 (a)、CUI-0025
- **問題**：`points is not None and points < 0` —— `-0.0 < 0` 係 `False`，所以 `-0.0` **唔 raise**；
  而 `-0.0` falsy，行落 `min(points, MAX) if points else MAX` 個 else 分支
  ⇒ **回成條 track（實測 600 行）**。同一函數 `-0.5` 同 `-1` 都 raise。
  另外 `points=2.5` 掟 `TypeError: 'float' object cannot be interpreted as an integer`，
  但 docstring 個 `Raises:` 只寫 `ValueError`。
- **影響**：到唔到 client（`_track_points` / `checkPoints` / `Int` coercion 三重都先 refuse），
  純粹係 public module-level function 嘅契約整潔。
- **建議**：guard 改 `not (points > 0)`（同時收 `-0.0`），或者明文寫低 `-0.0` 當 `0`；
  `Raises:` 補 `TypeError`（或者收窄成 `ValueError`）。
- **驗收**：`tracks(s,[id],-0.0)` 同 `tracks(s,[id],2.5)` 各有一條測試釘住決定。
  （順帶：`tracks(s,[],-1)` **已經**正確 raise —— guard 排喺 early-return 前，值得順手補一條釘住。）

### CUI-0049 🟡 Medium —— dev 樹 14 條 advisory 喺 CUI-0036 搬走之後冇咗歸屬
- **類型**：security / process gap ｜ **負責人**：DevOps Engineer ｜ **相關**：CUI-0036、CUI-0044
- **問題**：`pages.yml` 個註釋寫嗰 14 條「are tracked separately in **CUI-0036**」，
  但 CUI-0036 本身已標 done 準備搬去 `completed/`。2026-09-17 實測仍然存在：
  `npm audit` → **14 vulnerabilities (2 moderate, 12 high, 0 critical)**，
  全部經 devDependencies（`@graphql-codegen/*` → `lodash`、`vitest` → `@vitest/mocker`）。
- **建議**：bump `@graphql-codegen/*` 同 `vitest`，然後**剷走 `pages.yml` 嗰個 `--omit=dev` flag**
  （註釋自己講「dropping the flag is the point, not a regression」），並同步更新 `CLAUDE.md` §3。
- **驗收**：`npm audit`（全樹）exit 0；`pages.yml` 嗰步變 `npm audit`。

### CUI-0050 🟢 Low —— 四條 list field 嘅 `ARGUMENT_OUT_OF_RANGE` 分唔開 `limit` 定 `offset`
- **類型**：API contract ｜ **負責人**：Backend Developer ｜ **相關**：CUI-0018 (a)、W-035
- **問題**：W-035 選咗方案 A（改措辭），方案 B（`extensions.argument`）**另飛但冇開票**，
  而家只記錄喺 `reviews/2026-09-17_review_CUI-0018.md` 同 registry 入面。我獨立重現：

  | document | path | locations | code | message |
  |---|---|---|---|---|
  | `activities(limit:1000, offset:-1)` | `["activities"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `offset must not be negative, got -1` |
  | `activities(limit:-5, offset:0)` | `["activities"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `limit must be between 1 and 1000, got -5` |
  | `weight(limit:-5, offset:0)` | `["weight"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `limit …` |
  | `weight(limit:500, offset:-1)` | `["weight"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `offset …` |

  `path` / `locations` / `code` **三樣一模一樣**，只有 `message` 唔同 ⇒ client 照 SDL 自己嘅忠告
  「Branch on the code rather than on the message」去做，**係做唔到修正嘅**。
- **開票理由**：純粹係唔好讓一個刻意 defer 嘅決定只活喺 review report 入面（SSoT 係 `.tickets/`）。
  **唔 block release**，等到「真係有 client 要 branch」先做。

### CUI-0051 🟢 Low（docs）—— CUI-0038 測試註釋引咗 byte 數但冇講量邊個 document
- **類型**：docs / reproducibility ｜ **負責人**：Backend Developer ｜ **相關**：CUI-0038、S-068、S-069
- **問題**：`tests/test_api.py` 寫「at WARNING one bounds refusal writes **103** bytes … and one budget
  refusal **341**」。byte 數 100% 由 document 決定，而註釋冇講用邊個。我量到 102/332（`activities(offset:-1)`
  + 5×`activities(limit:1000)`）同 161/239（`track(points:0)` + 65-activity list），兩組都唔等於 103/341。
- **建議**：同 S-068 一樣加返 document 原文（或者索性剷走 byte 數，留返「非零 vs 零」呢個定性結論）。

---

## 建議補測（QA 唔可以自己 commit，請 invoke developer）

### 1. 🔺 最有價值 —— `graphql-request` 拒絕行為（`frontend/src/data/api/source.test.ts`）
`source.ts` / `types.ts` 嘅「no caller grew a null check」整個結論，靠嘅係
「refusal 一定帶 `errors`，`graphql-request` 一定 reject」呢個前提，而**全批冇一條測試守住**。
一旦唔成立，被拒 track 就會**靜靜哋畫個空 chart** 而唔係出 `TrackError`。
我已對住真 library 寫好 4 條並跑綠，原文（可直接落地）：

```ts
// 對住真 graphql-request，用 stub fetch 餵真 wire shape
it("REJECTS on partial data + errors (a refused track)", async () => { … });  // ✅ 綠
it("REJECTS on a bounds refusal of track too", async () => { … });            // ✅ 綠
it("resolves [] for an unknown id (no errors at all)", async () => { … });    // ✅ 綠
it("the refusal's extensions.code survives onto the thrown error", async () => { … }); // ✅ 綠
```
（第 4 條額外證實 `err.response.errors[0].extensions.code === "TRACK_FIELD_BUDGET_EXCEEDED"`，
即 CUI-0018 (a) 買嘅 code **真係去到前端 error object**，client 可以 branch。）

### 2. `tests/test_api.py` —— budget 邊界四點
`limit=64/points=1` 服務、`limit=65/points=1` → FIELD、`limit=10/points=1000` 服務、
`limit=11/points=1000` → POINTS。現有測試釘咗 field cap，但**冇釘住「邊個 code 喺邊個邊界出」**。
順手釘埋 `limit=65/points=1000` 出 **55 條 POINTS 而唔係 FIELD**（points budget 唔黐底嘅直接證據）。

### 3. `tests/test_api.py` —— session teardown 兩條
`/api/health` 開 0 個 session（`g.pop` 個 default 真係要）；resolver 掟嘢嘅 request 仍然閂 session
（`teardown_request` 相對 `call_on_close` 嘅真着數，而家冇測試講得出嚟）。

### 4. `tests/test_weather_collectors.py` —— icon code 第二宗
`_icon_code("n(26")`（只有 prefix）：舊算術出 `'2'` → **Few clouds**，唔係 Unknown。
票同 fixture 都只 cover 咗 `drawIcon(7)` → Rain 嗰宗。

---

## 📌 你要我確認嘅兩件事

### 1. v3.3.0 CHANGELOG 嘅 public 行為改動 —— 逐項核實 + 補漏

**你已知嗰 5 項，全部核實成立：**

| # | 項目 | 我量到嘅證據 |
|---|---|---|
| 1 | `track` 改 nullable（**BREAKING**）| SDL `track(points: Int! = 150): [TrackPoint!]`；被拒只 null 一個 field，兄弟存活；`activities` 維持 `[Activity!]!` |
| 2 | `run365-weather` exit code 語義 | 舊 `main()` **完全冇 try/except** ⇒ 第一個 `RequestException` 直接拋出，**後面啲 source 永遠唔會試**。新版 per-source 隔離、失敗 `logger.warning(exc_info=True)`、`run()` 回失敗數、`main()` 任何非零 → `SystemExit(1)` |
| 3 | `WeatherPageStructureError` 新失敗模式 | `run365days.weather.collectors.__all__ == ["WeatherPageStructureError"]`（**package 唯一 public export**）；`warnings.fetch_day` / `fetch_range` 兩個 docstring 都宣告咗 |
| 4 | `finite_float` 由 public API 消失 | `hasattr(numeric,"finite_float") == False`；真 caller 0 |
| 5 | `haversine_distance` 新增 `ValueError` | 三個非有限 case 實測 raise |

**我核實到、但你張清單漏咗嘅 7 項（建議加入）：**

| # | 項目 | 證據 |
|---|---|---|
| 6 | **🔺 每個 client refusal 新增 `extensions.code`（四個值）** | 全新 wire 契約，**比 #1 更加係加法**：`LIST_ROW_BUDGET_EXCEEDED` / `TRACK_POINTS_BUDGET_EXCEEDED` / `TRACK_FIELD_BUDGET_EXCEEDED` / `ARGUMENT_OUT_OF_RANGE`。`extensions` 內**只有** `code`，fault / coercion 一律**冇** code |
| 7 | **`hourly.fetch_day`：weather cell 讀唔到唔再拖冧成日** | 舊 `tds[9].find("script").string` 冇 script 即 `AttributeError` ⇒ **成日 0 record**。新版該 row 照出，`description="Unknown"`。實測 `freemeteo_no_script.html` 0 → **1 record** |
| 8 | **`warnings.fetch_day`：signal icon `<img>` 唔見咗唔再拖冧成日** | 舊 `tds[0].find("img").get(...)` → `AttributeError`。實測 `hko_warning_day_no_icon.html` 0 → **1 record**，`icon_url=''` |
| 9 | **hourly 數值：cell 冇單位唔再變 `None`** | `'Northeast 50° 24'` None→**24.0**、`'Variable at 20'` None→**20.0**、`'11'` None→**11.0**。**會改到重新收集出嚟嘅數據**，唔淨係內部整理 |
| 10 | `run365days.common.config` 新增 `HTTP_CONNECT_TIMEOUT_SEC` / `HTTP_READ_TIMEOUT_SEC` / `HTTP_REQUEST_TIMEOUT` | public 加法（舊嗰啲 `_REQUEST_TIMEOUT` 係 `_` 開頭，移除唔算 break）|
| 11 | `service.tracks(points=<負數>)` 改 raise `ValueError` | public module-level function 契約改變 |
| 12 | Operator-facing：`tag-release.yml` 新增 `notes_only` input；CI 新增 `npm audit --omit=dev` gate | 兩樣都係人手操作介面 |

**建議**放入「內部 / 無 user-visible 改動」而**唔好**當 public 行為改動嘅一項：
**CUI-0005 session lifecycle 由 `call_on_close` 改 `teardown_request`** ——
我實測真 WSGI server **修前修後都係 40 開 40 閂 0 洩漏**，production 由頭到尾正確；
改嘅係令 `test_client` 同真 server 一致。寫成 public behaviour change 會誤導。

### 2. 13 張標咗 `✅ done` 但仲喺 `.tickets/pending/` 嘅票 —— 可唔可以搬？

**可以，13 張全部可以搬去 `.tickets/completed/0001-0200/`**：
`CUI-0005`、`0008`、`0012`、`0013`、`0018`、`0035`、`0036`、`0037`、`0038`、`0039`、`0040`、`0041`、`0042`

**唯一條件**：`CUI-0036` 請**先開 CUI-0049** 承接 dev 樹嗰 14 條 advisory，再搬。
唔係嘅話 `pages.yml` 個註釋會指住一張已 close 嘅票，14 條 advisory 冇咗 owner
（同時建議搬完之後順手更新 `pages.yml` 嗰句 `tracked separately in CUI-0036` → `CUI-0049`，
不過咁做要改代碼，交返 developer，唔係 QA 做）。

`pending/` 搬完之後應該淨返 **4 + 5 = 9 張**：
原有 `CUI-0043` / `0044` / `0045` / `0046`，加新開嘅 `CUI-0047` … `CUI-0051`。
順帶：`CUI-0045` 條票入面「3,166 行 / 197 個測試」請更新做 **3,543 行 / 228 個測試**（我實測）。

---

## 阻止部署條件檢查

| 條件 | 狀態 |
|---|---|
| 🔴 任何 Critical 測試失敗 | ✅ 無 |
| 核心業務流程測試失敗 | ✅ 無（529 + 143 全綠）|
| 安全測試失敗 | ✅ 無（log 洩漏面實測 0 bytes；`npm audit --omit=dev` 0 條）|
| 數據一致性測試失敗 | ✅ 無（真 134,041 行 export 端到端）|

**不予阻止。**

---

```handoff-receipt
protocol: 1
status: pass
score: 93/100
hard_gates:
  lint: pass
  type_check: pass
  tests: pass
  coverage: "96%"
next_action: merge_develop
next_agent: null
branch: "claude/ai-dev-team-start-05jie2"
context: "14 tickets QA'd on 71dad80, all pass and all movable to completed. Independently measured: 529 pytest / 143 vitest (25 files) / TOTAL 96%, api schema+service+graphql all 100%. Log-leak surface measured OS-level on fd 2 against the real api/graphql.py + real 134,041-row export under Vercel-default logging: all 4 bounds paths and all 3 budget refusals 0 bytes; resolver fault 2343B/9 frames (confirmed by a planted marker), Int coercion 179B/207B/0 frames, unseeded RuntimeError 2776B/10 frames all retained. CUI-0018(b) verified both build modes, static/api parity, sibling survival, and the FIELD-vs-POINTS boundary (64/65 x1pt, 10/11 x1000pt); extensions carries code and nothing else, checked on raw response text. CUI-0012 marker -1 defect independently reproduced and the new fixture proven necessary. CUI-0005 verified at mechanism level on a real WSGI server: fixed=40 opened/40 closed on both transports, pre-fix test_client=40 opened/0 closed. CUI-0042 validated statically only -- NO workflow was dispatched. 5 new tickets CUI-0047..CUI-0051, all Low/Medium, none blocking. 3 upstream claims corrected: CUI-0038 test comment byte figures (103/341 -> 102/332, document unnamed), CUI-0045 counts (3,166/197 -> 3,543/228), and a measurement trap (65 aliased activity(id:) roots hit MaxTokensLimiter, not the field budget). CUI-0036 may move to completed only after CUI-0049 is opened to inherit the 14 dev-tree advisories. Working tree left pristine: git status empty, all mutated files md5-verified restored, no git operation performed."
blockers:
  - "CUI-0036 bookkeeping only: open CUI-0049 first so the 14 devDependency advisories keep an owner before CUI-0036 moves to completed/ (not a code defect, does not block merge)"
```
