# QA — 2026-09-14 — AU-047 per-request track budget（points + fields）

**測試員**：QA Agent（獨立 subagent，未參與實作，未參與兩輪 review）
**Branch / commit**：`claude/run365days-au047-resolver-8wg7ay` @ `c42ed6a`
（測試途中 main agent 加咗 `7bf7d92 docs: record the AU-047 second review`，
**已核實純 docs，零 `src/` `tests/` `frontend/` 改動**，本報告全部結果照樣成立）
**基準**：`origin/develop` @ `09d9ccc`
**測試環境**：本機 checkout、Python 3.11.15、`.venv`、Node 22 / vite 7.3.6、
真實資料集（365 TCX / GPX、363 天 HKO 天氣、461 條警告、365 次磅重）
**測試日期**：2026-09-14

---

## 測試計劃 — 點解唔重複 reviewer 做過嘅嘢

兩輪 review 已經深入驗證咗**實作層**：alias flood 19,764 → 130 SQL、9 種 context 形狀
fail-closed、5 個 mutation test 全捉、三個 commit 各自獨立綠。呢啲**唔再重複**。

本輪 QA 問嘅係另一條問題：**呢個改動有冇整爛個產品。**

| # | 範圍 | 點解要測 | 手法 |
|---|---|---|---|
| S1 | 真實數據 export 回歸 | AU-047 只改 `src/api/`，理論上唔應該影響 export；**有任何差異就係 red flag** | 基準 vs HEAD 全量 byte diff |
| S2 | 真實前端 8 條 document | 收緊限制最易誤傷自己人；reviewer 只跑過 fixture | 由 `queries.ts` 逐條抽出，打真 HTTP，對真 `run365.db` |
| S3 | static vs api 兩個 mode 一致性 | 兩條獨立輸出路徑，AU-047 只動其中一條 | 365 條 track 逐點對值 |
| S4 | 邊界同錯誤體驗（user-facing） | Bound 守得住 ≠ 用戶撞到嗰陣有得救 | 真 client response 逐 byte 睇 + 前端 code path 追蹤 |
| S5 | 主動測試（強制） | 298 條測試綠 ≠ 冇盲點 | 7 條全新 edge case，針對未覆蓋嘅形狀 |
| S6 | 全套回歸 | reviewer **冇跑過** frontend `lint` / `vitest` / 兩個 build | 全部補跑 |

**主動測試（強制）**：S5 七條 case 全部係新寫，唔係跑現有 298 條測試。
針對嘅係現有 AU-047 測試（`tests/test_api.py:488-684`，22 條）冇覆蓋嘅形狀：
fragment、inline fragment、nullable parent fan-out、同一 parent 嘅 track alias、
跨 budget fail-closed、`@skip` directive、points budget 嘅整除邊界。

---

## Hard Gates 結果

| Gate | 檢測方式 | 結果 |
|------|---------|------|
| **Tests（Python）** | `.venv/bin/python -m pytest tests -q` | ✅ pass — **298 passed in 8.14s** |
| **Tests（Frontend）** | `npx vitest run` | ✅ pass — **19 files / 87 tests passed** |
| **Lint（Python）** | `.venv/bin/ruff check src tests` | ✅ pass — All checks passed! |
| **Format** | `.venv/bin/ruff format --check src tests` | ✅ pass — 58 files already formatted |
| **Lint（Frontend）** | `npm run lint` | ✅ pass — eslint 零輸出，exit 0 |
| **Typecheck** | `npm run typecheck`（codegen + `tsc -b`） | ✅ pass — exit 0，codegen 無 drift |
| **Schema sync** | `.venv/bin/run365-schema --check frontend/schema.graphql` | ✅ pass — up to date |
| **Build（api mode）** | `npm run build` | ✅ pass — 292 modules，657.73 kB |
| **Build（static mode）** | `npm run build:static` | ✅ pass — 292 modules，657.69 kB |
| **Data integrity** | 基準 vs HEAD export 全量比對 | ✅ pass — **369/370 byte-identical**，只差 `generated_at` |
| **Performance** | 8 條真實 query × 30 次，P95 | ✅ pass — 全部 ≤ 76 ms；最壞 request 174 ms（Vercel 15 s，**86× headroom**） |
| **Security** | Budget 繞過嘗試 7 條（S5） | ✅ pass — 冇一條繞得過 |
| **No Critical** | 🔴 Critical 數量 | ✅ pass — **0** |

> **Coverage**：⚪ n/a —— `pytest-cov` 未安裝，`pyproject.toml` 亦無 coverage gate
> （同兩輪 review 一致）。AU-047 嘅 22 條測試 + 本輪 7 條 edge case 對
> `_charge_track_field` / `_TrackBudget` 兩個新 symbol 嘅分支覆蓋，經人手核對為完整
> （budget 足 / 剛好 / 不足 × fields / points × seeded / 未 seeded）。

---

## S1. 真實數據 export 回歸 —— byte-identical 守住咗

AU-047 三個 commit 嘅 diff 範圍（`git diff --stat 31edf32..c42ed6a`）：

```
 .proj-docs/index.md                            |   7 +-
 .proj-docs/reviews/2026-09-14_review_au-047.md | 182 +++++
 .proj-docs/tickets.md                          |  35 ++
 frontend/schema.graphql                        |   4 +-
 src/api/schema.py                              | 177 +++-
 tests/test_api.py                              | 216 +++++
```

**`src/` 之下只有 `api/schema.py`**，export pipeline（`activities/` `weather/` `weight/`
`dashboard/` `export/` `common/`）零改動。

實跑驗證，而唔係靠推論。基準用 `git archive origin/develop` 抽去 scratchpad
（**冇做任何 git 切 branch / commit 操作**），以 `PYTHONPATH` shadow 掉 editable install，
已確認 baseline 真係跑緊舊 code（`hasattr(schema, 'MAX_TRACK_FIELDS_PER_REQUEST') == False`）。

```
HEAD     : Records: 365 activities, 365 weigh-ins, 363 weather days, 461 warnings
           Wrote run365.db (11,460 KB) / static (6,696 KB)
baseline : Records: 365 activities, 365 weigh-ins, 363 weather days, 461 warnings
           Wrote run365.db (11,460 KB) / static (6,696 KB)
```

| 指標 | `.proj-docs/tickets.md` 記錄嘅基準 | 本輪 HEAD 實測 | 結果 |
|---|---|---|---|
| Activities | 365 | 365 | ✅ |
| SQLite 大小 | 11,460 KB | 11,460 KB（11,735,040 bytes） | ✅ |
| Static JSON 總 bytes | 6,857,102 | **6,857,102** | ✅ |
| Static JSON 檔案數 | 370 | **370** | ✅ |

全量 byte diff：

```
$ diff -r baseline_out/static data/processed/static
diff -r .../meta.json .../meta.json
1c1
< {"year":2021,"generated_at":"2026-09-14T13:31:05","counts":{...}}
> {"year":2021,"generated_at":"2026-09-14T13:29:47","counts":{...}}
```

md5 逐檔對拍：**369 / 370 完全相同**，唯一有差異嘅 `meta.json` 只差時間戳。
SQLite 兩邊 size 相同（11,735,040 bytes），row count 相同
（activities 365、track_points 134,041、daily_weather 363、weather_warnings 461、meta 2）。

> ✅ **結論：byte-identical 基準守住。AU-047 對 export 零影響，冇 red flag。**

---

## S2. 真實前端 document 端到端

由 `frontend/src/data/api/queries.ts` 逐條抽出（連 `ActivityFields` fragment），
用 `create_app(db_path="data/processed/run365.db")` 起真 Flask app，打真 HTTP POST。
**唔係 fixture，係啱啱 export 出嚟嗰個真 DB。**

```
real activity id under test: 6077489710 (numPoints=390)

document           http  errors  sql          ms  payload
------------------------------------------------------------------------------
MetaQuery          200   0       1           3.6  67 bytes
YearQuery          200   0       3          33.9  61,009 bytes
ActivitiesQuery    200   0       2          40.1  145,011 bytes
ActivityQuery      200   0       2           7.0  506 bytes
TrackQuery         200   0       4          20.2  47,991 bytes  track_points=390
WeightQuery        200   0       1          42.0  25,150 bytes
WeatherQuery       200   0       1          17.6  56,708 bytes
WarningsQuery      200   0       1          13.4  71,743 bytes

ALL 8 REAL FRONTEND DOCUMENTS: 200, errors=[]
```

### TrackQuery 用真實參數攞唔攞到足數點

前端寫死 `TRACK_POINTS = 600`（`ActivityView.tsx:17`，`source.ts:20` default 同樣 600）。
**全 codebase 只有一個 `useTrack` call site**，即一個 request 得 1 個 `track` field。

對**全部 365 條 track** 核實 `track(points: 600)` 嘅回傳點數：

```
stored points per track: min=250  median=366  max=600   (at 600 ceiling: 2 條)
api track(points:600) != min(stored, 600) : 0 條
```

✅ 每一條 track 都攞足 —— 冇一條被 budget 削過。600 點 × 1 field 距離
`MAX_TRACK_POINTS_PER_REQUEST = 10000`（16.7×）同 `MAX_TRACK_FIELDS_PER_REQUEST = 64`（64×）
都好遠。

### YearQuery 嘅 track 消耗（核實 reviewer 講法）

唔靠讀 code，直接 patch `_charge_track_field` 數實際 call 次數：

```
track fields charged by YearQuery: 0   sql=3  31.4 ms  errors=0
```

✅ **核實：`YearQuery` 消耗 0 個 track field、0 點。** `personalBests` 五個 field
全部只 spread `ActivityFields`，而該 fragment 由頭到尾冇 `track`。W-012 嘅判斷正確。

---

## S3. Static mode vs API mode 一致性

Static 嘅 track 係 columnar `{columns, rows}`；API 係 object list。逐點還原後對值。

```
activities compared         : 365
stored points min/med/max   : 250 / 366 / 600
point-COUNT mismatches      : 0
point-VALUE mismatches      : 0     (sec / lat / lon 逐點對，float 取 9 位)
api600 != min(stored, 600)  : 0

=> static mode 同 api mode 喺每一條 track 上逐點一致
```

### S-011（已登記既有差異）—— 有冇惡化？

```
api    points:0 -> http=200 data={'activity': None}
                   errors=['points must be between 1 and 1000, got 0']
static points:0 -> 冇 server call；tracks/<id>.json 本身已經載住全部 250 行
```

✅ 行為同登記時完全一致：api mode 拒絕 `points: 0`，static mode 冇呢個概念。
**AU-047 冇令 S-011 惡化** —— 拒絕 `points: 0` 嘅係 `_track_points()`，
一路以來都喺度，唔係新加嘅 budget。

---

## S4. 邊界同錯誤體驗（user-facing）

### B1. 撞到 field cap 嗰陣 client 收到乜

```
limit=64  http=200 sql=130  data=present  errors=0  resp=3,925 bytes
limit=65  http=200 sql=130  data=null     errors=1  resp=177 bytes
   message : track field budget exhausted: one request may read at most 64 tracks
   keys    : ['locations', 'message', 'path']
   path    : ['activities', 64, 'track']
   body    : {"data":null,"errors":[{"message":"track field budget exhausted: one request
              may read at most 64 tracks","locations":[{"line":1,"column":61}],
              "path":["activities",64,"track"]}]}
```

| 問題 | 答案 |
|---|---|
| 解釋到點解被拒？ | ✅ 訊息直接講出原因同確切上限（64） |
| 解釋到點樣改個 query？ | ✅ `path: ["activities", 64, "track"]` 準確指到第 65 個（index 64），減到 64 就得 |
| HTTP status？ | 200 —— GraphQL 慣例，正確 |
| 有冇洩漏 internal detail？ | ✅ **冇**。177 bytes、無 stacktrace、無絕對路徑、無 context key 名 |

W-013 特登唔講出 `track_points_remaining` 呢個 key 係做得啱嘅，實測確認冇洩漏。
（server 端 log 仍有完整 traceback —— 屬已登記嘅 **S-014 / S-018**，唔喺 AU-047 範圍。）

**但**冇 `extensions.code`，client 想 branch 就只能 match 字串，而個字串由常數 f-string
砌成，常數一改就靜靜哋失效 → **CUI-0018**。

### B2. 一個被拒 track 會唔會拖冧兄弟 field

```
query { meta { year } activities(limit: 65, hasGps: true) { id distanceKm track(points:1){sec} } }
-> http=200  data=null (EVERYTHING lost)  連 meta 都冇
```

`activities: [Activity!]!` 同 `track: [TrackPoint!]!` 都係 non-null，一拒就 propagate 到 root。
Nullable parent 溫和好多：

```
a: activity(id:"...") { track(points: 1200) { sec } }
-> data={"a": null}  errors=['points must be between 1 and 1000, got 1200']   兄弟照返
```

呢個係 GraphQL non-null 語義嘅必然結果，**唔係 AU-047 造成**，今日前端亦撞唔到
（1 個 track field）。但 AU-050 之後會變常態 → 一併記入 **CUI-0018**。

### B3. 前端真係撞到會點？會唔會白畫面？

追 `ActivityView.tsx` 嘅 code path：

```
33:  const track = useTrack(current, TRACK_POINTS);
37:    () => (activity.data && track.data ? buildSeries(activity.data, track.data) : null),
69:  if (all.isPending || activity.isPending || track.isPending) return <p>Loading…</p>;
70:  if (all.isError) return <p>Could not load activities: {all.error.message}</p>;
71:  if (!activity.data || !series) return <p>Activity not found.</p>;
```

`track` 失敗 → `isPending=false`、`data=undefined` → `series=null` → **行 71 fire**。

| 問題 | 答案 |
|---|---|
| 白畫面？ | ❌ **唔會**。行 71 係乾淨 early return，React 唔會 throw，冇需要 ErrorBoundary |
| 顯示乜？ | **「Activity not found.」** —— 但 activity 明明存在，`activity.data` 已經成功載入 |
| 有冇 retry？ | ❌ 冇 |

`all.isError`（行 70）有處理，**唯獨 `track.isError` 冇**。訊息報錯咗因 → **CUI-0016**。

> ⚠️ 呢個**唔係 AU-047 引入**（任何 track 失敗一直都咁），今日亦**撞唔到 budget**。
> 但 AU-047 為呢條路徑新增咗一個成因，AU-050 之後會變真實可達。

### B4. 兩個 budget 邊個先 fire

```
64 x points:150 (= 9,600 pts，兩個 budget 都啱啱入面)  sql=130   271 ms  errors=0  ✅ 服務
64 x points:1000 (= 64,000 pts，points 爆)             sql=22     59 ms  errors=1  points budget
365 x points:1 (field cap 爆)                          sql=130   125 ms  errors=1  field cap
```

✅ 兩個 budget 真係正交：points 爆嗰次只行 22 條 SQL（未到 64 個 field 就已經拒絕），
field cap 爆嗰次穩定喺 130 條。冇任何一條組合可以兩個都繞過。

### B5. C-001 嗰條 alias flood，喺出貨 code 上重量

```
27 aliases x activities(365){track(1)} : http=200  sql=130  106 ms  errors=1
（review 喺加 field cap 之前量到 19,764 SQL / ~14 s）
```

✅ **19,764 → 130 條語句（152× 減少）；~14 s → 106 ms（~130× 減少）。**
C-001 喺真實 production-shaped 數據上確認修好。

### B6. `MAX_TRACK_FIELDS_PER_REQUEST = 64` 對將來功能嘅影響

| 情境 | 今日會點 | 評估 |
|---|---|---|
| 一頁畫全年 365 條 route | ❌ 被拒（365 > 64） | 已知設計決定，AU-050 會加 batch field |
| 一頁畫一個月（~30 條 route） | ✅ **過到**（30 < 64） | 最貼近嘅真實情境，安全 |
| 一頁畫一週（~7 條） | ✅ 過到 | 安全 |
| 對比 2–5 條 run 嘅 route / 配速 | ✅ 過到 | 安全 |
| Activities 列表加 route 縮圖（365 行） | ❌ 被拒 | **最容易撞板嘅真實情境** |
| 現時前端全部 8 條 document | ✅ 過到（最多 1 個 track field） | 零影響 |

> **評估結論**：64 對「月 / 週 / 對比」呢類最貼近嘅真實需求有足夠空間
> （最密嘅月份都遠低於 64）。唯一實際會撞板嘅係「列表逐行縮圖」，
> 而嗰種情境本身就應該行 AU-050 嘅 batch field 而唔係 365 個獨立 round trip。
> **64 呢個數值 QA 認為合理，唔開 ticket。**

---

## S5. 主動測試 —— 7 條全新 edge case

全部針對現有 22 條 AU-047 測試冇覆蓋嘅形狀。對真實 365-activity DB 跑。

| # | Edge case | 結果 |
|---|---|---|
| E1 | `track` 經**具名 fragment** 到達，仲收唔收費？ | ✅ 收 —— `sql=130`，cap 照 fire |
| E2 | `track` 經 **inline fragment** 到達？ | ✅ 收 —— `sql=130`，cap 照 fire |
| E3 | **Nullable parent fan-out**（aliased `activity(id:)`，被拒唔會中斷 request） | ⚠️ 有發現，見下 |
| E4 | 同一個 parent 上 65 個 `track` **alias**（GraphQL field merging 會唔會變免費？） | ✅ 每個照收一格，cap fire |
| E5 | **跨 budget fail-closed**：points 先耗盡，之後嘅平價 track 仲拒唔拒？ | ✅ 20 個平價 alias **全部拒絕**，`served=0` |
| E6 | `@skip(if: true)` 嘅 track 會唔會被**誤收費**（誤殺合法 query）？ | ✅ 唔收 —— `errors=[] sql=2`，357 行照返 |
| E7 | Points budget 整除邊界：11 × `points:1000` | ✅ 啱啱好服務 10 條（`10000 // 1000`），第 11 拒絕 |

**7 條全部通過**（E6 第一次跑報 FAIL 係我自己個 assertion 寫錯 —— 我預期 365 行，
實際 `activitiesCount(hasGps: true) = 357`。已核實 357 先係正確值，code 冇問題）。

### E5 值得特別講（最有價值嗰條）

`_charge_track_field` 先算 `fields_left` 同 `points_left`，**兩個都 ≥ 0 先一齊寫返落 context**。
即係話 points 爆咗嗰次，**兩個 counter 都唔會更新**。我原本懷疑呢度可以造成
「失敗嘅收費唔扣數 → 之後嘅平價 field 又有得行」嘅繞過。

實測否定咗呢個懷疑：points budget 一旦見底就**永遠唔會回復**，
所以之後每一個 `track` 都繼續拒絕，而且每個都喺出 SQL 之前拒絕。
**Fail-closed 成立，冇繞過。**

### E3 —— 唯一一個發現（🟢 Minor）

`activity(id:)` 係 nullable，被拒嘅 track 只會 null 自己個 alias，request 繼續行。
二分搜尋 `MAX_QUERY_TOKENS = 1000` 容許嘅最大 alias 數，再量語句：

```
max aliases accepted by the token limiter : 52
tracks served                             : 52 (field cap = 64)
TOTAL SQL                                 : 208   wall 143 ms
breakdown: 52 parent lookups x 2 = 104, plus 52 served tracks x 2 = 104  -> 208
```

`src/api/schema.py:84` 寫「Worst case is therefore 64 x 2 = 128 statements」，
但每個 aliased `activity(id:)` **parent 本身就要 ~2 條語句**（`session.get` +
`selectinload(warnings)`），**而且個 track 就算被拒都照收**。呢半邊唔受 field cap 約束，
只受 token limiter 約束。實測 request 層面 worst case 係 **208**，唔係 128。

> **Bound 本身冇問題** —— 208 條 / 174 ms 對 15 s 有 86 倍 headroom。
> 純粹係 docstring 嘅數字同實測對唔上 → **CUI-0017**。
> 因為 AU-047 兩輪之中已經有兩個 finding（C-001、W-012）都係同一類偏差，
> 而 `6a040f1` 成個 commit 就係為咗修正被實測否證嘅理據，所以值得留低一條。

---

## S6. 全套回歸 —— reviewer 冇跑過嗰幾樣

```
$ .venv/bin/python -m pytest tests -q
298 passed in 8.14s

$ .venv/bin/ruff check src tests
All checks passed!

$ .venv/bin/ruff format --check src tests
58 files already formatted

$ .venv/bin/run365-schema --check frontend/schema.graphql
frontend/schema.graphql is up to date
```

以下四項 **reviewer 冇跑過**（佢只跑咗 `typecheck`），本輪補齊：

```
$ npm run lint                      # eslint . -> 零輸出，exit 0                    ✅ 新跑
$ npx vitest run                    # Test Files 19 passed / Tests 87 passed        ✅ 新跑
$ npm run build                     # 292 modules, 657.73 kB, built in 2.44s        ✅ 新跑
$ npm run build:static              # 292 modules, 657.69 kB, built in 2.60s        ✅ 新跑
$ npm run typecheck                 # codegen SUCCESS + tsc -b, exit 0, 無 drift     ✅ 覆跑
```

> `vitest` 輸出有 `Failed to create chart: can't acquire context from the given item`
> —— jsdom 冇 canvas context，Chart.js 喺測試環境嘅既有雜訊，**唔影響結果**
> （19/19 檔案、87/87 測試綠），亦同 AU-047 無關。

### 效能 / per-request 隔離（真實 DB）

```
document             n      p50      p95      max   errors
MetaQuery           30     2.0ms     2.7ms     3.4ms   0
YearQuery           30    31.7ms    62.0ms    67.8ms   0
ActivitiesQuery     30    43.0ms    76.3ms    78.8ms   0
ActivityQuery       30     4.9ms     7.1ms    35.8ms   0
TrackQuery          30    18.2ms    20.7ms    51.3ms   0
WeightQuery         30     7.7ms    11.3ms    38.4ms   0
WeatherQuery        30    13.6ms    16.1ms    41.8ms   0
WarningsQuery       30    12.2ms    17.3ms    53.0ms   0
total errors across 240 requests: 0

per-request budget isolation: 300/300 served at exactly the field cap（73.5s，245 ms each）
worst single request observed: 52-alias fan-out (208 SQL) p50=137 ms max=174 ms
Vercel 15,000 ms limit -> headroom 86x
```

✅ 240 個真實 request 零錯誤；300 個連續 full-cap request 全部服務，budget 冇跨 request 洩漏
（呢個係喺**真實 365-activity DB** 上覆核 reviewer 用 fixture 得出嘅同一結論）。

---

## 問題清單同優先順序

| 次序 | Ticket | 嚴重度 | 問題 | 阻唔阻 merge |
|---|---|---|---|---|
| 1 | **CUI-0016** | 🟡 Major | `ActivityView` 將 track 失敗誤報成「Activity not found.」 | ❌ 唔阻（pre-existing、今日撞唔到；AU-050 之前要修） |
| 2 | **CUI-0017** | 🟢 Minor | docstring worst case 寫 128，實測 208（漏計 parent lookup） | ❌ 唔阻 |
| 3 | **CUI-0018** | 🟢 Minor | Budget 拒絕冇 `extensions.code`；non-null propagation 清空 `data` | ❌ 唔阻 |

**🔴 Critical：0**

三張全部**唔阻 AU-047 merge**：CUI-0016 係 pre-existing 且今日不可達；
CUI-0017 純文件準確性（bound 本身有 86 倍 headroom）；CUI-0018 係 AU-050 之前嘅準備工作。

---

## 測試建議（下一步）

1. **AU-050 之前** 一次過修 CUI-0016 + CUI-0018 —— 兩者係同一條 user journey
   （budget 拒絕 → client 分辨 → 畫面顯示），分開修會做兩次同樣嘅上下文重建。
2. **補測建議（QA 唔可以自己 commit，請 main agent invoke developer）**：
   本輪 7 條 edge case 之中，以下 3 條建議收編入 `tests/test_api.py`，
   因為佢哋釘住嘅係現有 22 條測試冇釘住嘅行為：
   - **E5 跨 budget fail-closed** —— points 耗盡之後平價 track 仍然被拒。
     現時如果有人將 `_charge_track_field` 改成「失敗時只扣 field 唔扣 points」之類，
     298 條測試全部照綠。**價值最高嗰條。**
   - **E1 / E2 fragment** —— 釘住「fragment 唔係繞過 budget 嘅後門」。
     現有測試全部用直接 selection。
   - **E6 `@skip`** —— 釘住「directive 跳過嘅 field 唔會被誤收費」，防止將來有人
     改成 document-level 靜態計數（睇落更快）之後誤殺合法 query。
3. **Coverage gate**：`pytest-cov` 一直未安裝，`src/api/schema.py` 新增嗰 177 行
   冇客觀覆蓋率數字。建議裝返並喺 `pyproject.toml` 設 80% gate ——
   呢個係連續三份報告（兩輪 review + 本輪）都寫「⚪ n/a」嘅同一格。
4. **Export 回歸自動化**：`6,857,102 bytes / 370 files` 呢個基準一直靠人手重跑對比。
   考慮加一個 CI job 對 committed checksum manifest 比對，令 byte-identical 變成
   真 gate 而唔係每次人手做（同 `run365-schema --check` 同一 pattern）。

---

## 結論

AU-047 第二輪修復**通過 QA**。

- **Export 零影響**：369/370 byte-identical，只差 `generated_at`；365 activities /
  6,857,102 bytes / 370 files 三個基準全中。
- **產品冇整爛**：8 條真實前端 document 全部 200 / errors=[]；365 條 track 喺
  `points: 600` 之下逐條攞足；static 同 api 兩個 mode 逐點一致；`YearQuery` 實測消耗 0 個 track field。
- **C-001 真係修好咗**：真實數據上 19,764 SQL / ~14 s → 130 SQL / 106 ms。
- **7 條全新 edge case 冇一條繞得過** budget，包括 fragment、inline fragment、
  alias merging、nullable parent fan-out、跨 budget 耗盡。
- 發現 1 🟡 Major + 2 🟢 Minor，**全部唔阻 merge**，已開 CUI-0016 / 0017 / 0018。

---

## Handoff receipt

```HANDOFF_RECEIPT
protocol: 2
agent: quality-assurance
task: AU-047 per-request track budget (points + fields)
branch: claude/run365days-au047-resolver-8wg7ay
commit: c42ed6a (code) / 7bf7d92 (HEAD, docs-only)
baseline: origin/develop @ 09d9ccc
status: pass
next_action: merge_develop
critical_count: 0
major_count: 1
minor_count: 2
blocking_items: none
tickets_opened: CUI-0016, CUI-0017, CUI-0018
hard_gates:
  tests_python: pass (298 passed in 8.14s)
  tests_frontend: pass (19 files / 87 tests)
  lint_python: pass (ruff check - All checks passed)
  format_python: pass (ruff format --check - 58 files already formatted)
  lint_frontend: pass (eslint, exit 0)
  typecheck_frontend: pass (codegen + tsc -b, exit 0, no drift)
  schema_sync: pass (run365-schema --check - up to date)
  build_api_mode: pass (292 modules, 657.73 kB)
  build_static_mode: pass (292 modules, 657.69 kB)
  data_integrity: pass (369/370 static JSON byte-identical, only generated_at differs)
  performance: pass (P95 <= 76 ms on 8 real documents; worst request 174 ms, 86x headroom)
  security: pass (7/7 budget bypass attempts refused)
  no_critical: pass (0)
  coverage: n/a (pytest-cov not installed, no gate in pyproject.toml)
report: .proj-docs/qa/2026-09-14_qa_au-047.md
git_operations_performed: none
```
