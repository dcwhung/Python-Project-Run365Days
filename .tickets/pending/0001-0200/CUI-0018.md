# CUI-0018 Budget 拒絕冇 `extensions.code`，client 只能靠 string match；加上 non-null propagation，一個被拒 `track` 會清空成個 `data`

**狀態**：pending
**類型**：improvement
**優先級**：🟢 Low
**來源**：QA Report 2026-09-14（AU-047 `/test`，S4 錯誤體驗 B1 / B2）
**建立日期**：2026-09-14
**建立人**：QA Agent
**負責人**：Backend Developer（主）＋ Frontend Developer（配合）
**關聯 QA Report**：`.proj-docs/qa/2026-09-14_qa_au-047.md`（S4）
**關聯 Review Item**：`S-014` → `S-018`（error 處理整批改善）

---

## 問題描述

兩個獨立但同屬「budget 拒絕嘅 client 體驗」嘅缺口。

### (a) 錯誤冇機器可讀嘅分類

Budget 拒絕回嘅 error object 只有三條 key：

```json
{"data":null,"errors":[{
  "message":"track field budget exhausted: one request may read at most 64 tracks",
  "locations":[{"line":1,"column":61}],
  "path":["activities",64,"track"]}]}
```

```
keys : ['locations', 'message', 'path']
```

**冇 `extensions`，冇 `code`。** Client 想分辨「我個 query 太貪心，減少數量再試」
同「server 壞咗，唔好再試」，唯一辦法係 **match `message` 字串**。
而個訊息係由 `MAX_TRACK_FIELDS_PER_REQUEST` f-string 嵌入常數值砌出嚟，
常數一改字串就變 —— client 嘅 string match 會靜靜哋失效。

> 好消息（已核實）：response **冇洩漏任何 internal detail** ——
> 177 bytes、無 stacktrace、無絕對路徑、無 context key 名。
> W-013 特登唔講出 `track_points_remaining` 呢個 key 係做得啱嘅，本票唔推翻嗰個決定。
> 至於 server 端 log 仍然有完整 traceback，屬 **S-014 / S-018** 範圍，唔喺本票。

### (b) 一個被拒 `track` 會連兄弟 field 一齊清空

`activities: [Activity!]!` 同 `track: [TrackPoint!]!` 都係 non-null，
所以一個被拒嘅 `track` 會 non-null propagate 一路上到 root：

```graphql
query { meta { year }
        activities(limit: 65, hasGps: true) { id distanceKm track(points: 1) { sec } } }
```
→ `data: null`。**連完全成功嘅 `meta` 都冇埋。**

對比 nullable parent 就溫和得多：`activity(id:)` 之下被拒，`data: {"a": null}`，兄弟照返。

呢個係 GraphQL non-null 語義嘅必然結果，**唔係 AU-047 造成**，而且今日前端
（一個 request 只有 1 個 `track` field）撞唔到。但 AU-050 之後多 track 嘅 view 會變常態。

## 重現步驟

以真實 export 起 `create_app`，POST 落 `/api/graphql`：

```
limit=64  http=200 sql=130  data=present  errors=0  resp=3925 bytes
limit=65  http=200 sql=130  data=null     errors=1  resp=177 bytes
   message : track field budget exhausted: one request may read at most 64 tracks
   keys    : ['locations', 'message', 'path']
   path    : ['activities', 64, 'track']

加 sibling meta 之後：http=200 data=null (連 meta 都冇)
```

## 預期結果

1. Error 帶 `extensions.code`（例如 `TRACK_BUDGET_EXHAUSTED` / `TRACK_FIELD_BUDGET_EXHAUSTED`），
   client 可以穩定分辨而唔使 match 字串。
2. Budget 拒絕呢種「client 自己改得到」嘅錯誤，最好唔好連兄弟 field 一齊拖冧。

## 實際結果

1. 只有 `message` / `locations` / `path`，冇 `extensions`。
2. Non-null list 之下一個被拒 `track` 令成個 `data` 變 null。

## 環境

- **測試環境**：dev，真實 export（365 activities）
- **相關版本**：`c42ed6a`
- HTTP status 一律 **200**（GraphQL 慣例，正確，本票唔要求改）

## 影響範圍

- **受影響功能**：任何將來一次攞多過一條 track 嘅 view（AU-050 之後）
- **受影響用戶**：今日 **0**（前端每個 request 得 1 個 `track` field、600 點）
- **相關**：
  - `CUI-0016` —— 前端收到呢個錯誤之後顯示「Activity not found.」，兩票一齊修最順
  - `S-014` / `S-018` —— server log traceback，屬另一票
  - `AU-050` —— batch track field，落之前應該搞掂本票

## 建議修復方向

1. 定義一個 `TrackBudgetExhausted(Exception)`（或用 Strawberry 嘅 error extension 機制），
   喺 `_charge_track_field` 掟，令 error 帶 `extensions: {"code": "..."}`。
   **保持而家唔講 context key 名嘅做法。**
2. `TRACK_DESCRIPTION` 補一句講明拒絕會帶邊個 code，令 SDL 讀者知道點 branch
   （同 S-012 定落「常數值嵌入 SDL description」嘅 pattern 一致，
   `run365-schema --check` 會喺 CI 幫手守住同步）。
3. (b) 屬設計取捨，建議**只記錄唔急改**：真要改就要將 `track` 改成 nullable
   （`[TrackPoint!]` 而唔係 `[TrackPoint!]!`），屬 breaking schema change，
   要連 codegen 出嚟嘅前端 type 一齊評估 —— 應該同 AU-050 一併決定，唔好單獨做。

## 驗證方式

- 新測試：斷言被拒 response 有 `errors[0].extensions.code`，而且**冇**出現
  `track_points_remaining` / `track_fields_remaining` 呢兩個 key 名
- 新測試：斷言 response body 唔含 `Traceback` 同絕對路徑
- `run365-schema --check frontend/schema.graphql` 綠（SDL description 改咗就要同步）
- 298 條測試全綠

## 完成日期

—

---

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-14 | — | pending | QA Agent | 建立 ticket |
