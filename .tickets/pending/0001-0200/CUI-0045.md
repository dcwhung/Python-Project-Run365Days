# CUI-0045 `tests/test_api.py` 已經 3,166 行 / 197 個測試，Vercel entry 嘅測試應該拆出去

**狀態**：pending
**類型**：tech-debt
**優先級**：🟢 Low
**來源**：Code Review 2026-09-17（CUI-0036 batch，S-099）
**建立日期**：2026-09-17
**建立人**：Main Agent（由 Code Reviewer 發現）
**負責人**：Backend Developer
**相關**：CUI-0035（新加咗 5 條 entry 測試）、AU-001（原有兩條 GraphiQL entry 測試）

---

## 問題描述

`CLAUDE.md` §2 講「一個 feature module 一個檔案」。而 `api/graphql.py`（Vercel entry）
嚴格嚟講唔係 `src/api/` 嘅一部分 —— 佢係 deploy shim。

CUI-0035 加咗 5 條 entry 測試落 `tests/test_api.py`，放喺既有 `_load_vercel_entry`
（AU-001 加嘅）隔籬係合理嘅**局部**決定。但成個檔案而家係 **3,166 行 / 197 個測試**，
已經好難 navigate。

⚠️ Reviewer 明確講咗：**呢個係 pre-existing 問題，唔應該記喺 CUI-0035 嗰條 lane 數上。**

## 建議

抽 `tests/test_vercel_entry.py`，把下列嘢一齊搬過去：

- CUI-0035 嗰 5 條 start-up failure path 測試
- AU-001 嗰兩條 GraphiQL entry 測試
- `_load_vercel_entry` helper
- `VERCEL_ENTRY` / `GRAPHIQL_ENV` 等共用常數

⚠️ 搬之前要確認冇其他測試依賴嗰啲常數（`grep` 全檔）。

## 驗證方式

- 測試總數不變（搬唔係刪）
- `api/graphql.py` coverage 維持 **100%**
- 全 suite 綠、ruff clean
