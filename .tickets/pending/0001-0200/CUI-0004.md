# CUI-0004 `track(points: 1)` 只回最後一點，同 `_even_positions` docstring 嘅承諾相反，且係全 module 唯一未覆蓋嘅 line

**狀態**：pending
**類型**：bug
**優先級**：🟢 Low
**來源**：QA Report 2026-09-14
**建立日期**：2026-09-14 00:52
**建立人**：QA Agent
**負責人**：Backend Developer
**關聯 QA Report**：`.proj-docs/qa/2026-09-14_qa_p0-batch.md`（S2 / S17）
**Code Review 報告**：`.proj-docs/reviews/2026-09-13_review_p0-batch.md`（W-010）

---

## 問題描述

W-010 令 SQL 側嘅 stride 同 in-Python `downsample()` 完全一致（實測 0 分歧，見下）。
但 `points = 1` 呢個邊界之下，實際行為同 `_even_positions` 自己個 docstring 對唔上。

`src/api/service.py:136-152`：

```python
def _even_positions(total: int, points: int) -> list[int]:
    """...
    Returns:
        Strictly increasing positions, first and last row included.   <- 承諾
    """
    if points < 2:
        return [total - 1]            # <- line 150：只回最後一點，first 唔喺度
    ...
```

`points = 1` 時回 `[total - 1]`，即**淨係最後一點**，first row 唔包括在內。
GraphQL field description 亦寫「evenly downsampled to at most `points` samples」，
一個 sample 而家係「最後一個」而唔係「第一個」或者任何「evenly」嘅語義。

行為本身係**刻意**同 `run365days.dashboard.builder.downsample()` 對齊
（`if limit < 2: return [points[-1]]`），而個 module docstring 亦明講要對齊，
所以唔應該亂改行為 —— 但 docstring 嘅承諾同實際唔一致，係要修嘅嘢。

加上 coverage 顯示 **`src/api/service.py:150` 係成個 module 唯一冇被測試行過嘅 line**
（service.py 84 stmts / 1 miss / 99%）。即係話呢條同 docstring 矛盾嘅分支，
剛好亦係冇測試釘住嘅嗰條。

## 重現步驟

```
### S2  track(points:) on activity 6701104700 (600 stored points)
  points=1        returned=    1  first_sec=3417  last_sec=3417   <- 最後一點
  points=2        returned=    2  first_sec=0     last_sec=3417
  points=3        returned=    3  first_sec=0     last_sec=3417
  points=150      returned=  150  first_sec=0     last_sec=3417

### S17  _even_positions vs downsample
  0 divergence(s)（total ∈ {1,2,3,5,10,149,150,151,250,600,601,1000} × points ∈ {1,2,3,7,150,599,600,1000}）
  _even_positions(600, 1) = [599]   <- docstring 話 "first and last row included"
```

## 預期結果

以下三者一致：`_even_positions` docstring、GraphQL field description、實際行為。

## 實際結果

Docstring 承諾包含 first row；`points=1` 只回 last row。
`src/api/service.py:150` 冇任何測試覆蓋。

## 環境

- **測試環境**：dev，真實 DB（365 activities，最大 track 600 點，最細 250 點）
- **相關版本**：`08c7ea4`

## 影響範圍

- **受影響功能**：`Activity.track(points:)` resolver
- **受影響用戶**：任何傳 `points: 1` 嘅 client。前端 8 條 query 冇一條會咁做
  （`TrackQuery` 用 `DEFAULT_TRACK_POINTS = 150`），所以現時無實際影響
- **相關 ticket**：`S-011` 已記低 static mode 仍當 `points: 0` 為「攞全部」而 api mode 拒絕；
  呢張係同一片邊界地帶嘅另一個洞

## 截圖 / 錯誤訊息

```
$ pytest tests -q --cov=src --cov-report=term-missing
src/api/service.py    84    1    99%   150
                                       ^^^ if points < 2: return [total - 1]
```

## 子 Tickets（如有拆分）

無 —— 純 backend。

## 建議修復方向

1. 改 `_even_positions` docstring：`points >= 2` 先保證首尾都在；`points == 1` 回最後一點，
   並註明呢個係刻意同 `downsample()` 對齊（同 module docstring 已有嘅講法呼應）
2. GraphQL `track` field description 補一句 `points: 1` 嘅語義
3. 補測（呢張 ticket 嘅重點）：`_even_positions(total, 1)` 同 `track(points: 1)` 各一條，
   斷言回 1 行而且係最後一行 —— 令 line 150 有覆蓋，日後改動會被捉到
4. 如果團隊認為「1 個 sample 應該係第一點」更合理，咁就要連 `downsample()` 一齊改，
   屬行為改動，要另外評估對 static mode 嘅影響

## 驗證方式

- `pytest --cov=src` 之下 `src/api/service.py` 去到 100%
- 新測試斷言 `points=1` 嘅回傳長度同內容
- 現有 185 條測試全綠

## 完成日期

—

---

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-14 00:52 | — | pending | QA Agent | 建立 ticket |
