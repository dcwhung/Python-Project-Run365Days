# CUI-0049 dev 樹嗰 14 條 advisory 喺 CUI-0036 搬去 completed 之後冇咗歸屬

**狀態**：✅ done（2026-09-17）
**類型**：security / process gap
**優先級**：🟡 Medium
**來源**：QA 2026-09-17（CUI-0036 bookkeeping 核實）
**建立日期**：2026-09-17
**建立人**：Main Agent（由 QA Agent 發現）
**負責人**：DevOps Engineer
**相關**：CUI-0036（gate 部分已做）、CUI-0044（Python 側，唔 cover 呢個）

---

## 問題描述

`.github/workflows/pages.yml` 個 audit step 註釋寫住嗰 14 條 advisory
「are tracked separately in **CUI-0036**」。但 CUI-0036 本身已標 `✅ done`、準備搬去 `completed/`。

**一搬，14 條 advisory 就冇咗 owner，而註釋會指住一張已 close 嘅票。**

⚠️ CUI-0044 講嘅係 **Python 側 `pip-audit`**，唔 cover 呢個。

## 2026-09-17 實測（QA）

```
$ cd frontend && npm audit
14 vulnerabilities (2 moderate, 12 high, 0 critical)     exit 1
$ npm audit --omit=dev
found 0 vulnerabilities                                  exit 0
```

全部經 devDependencies 到達：`@graphql-codegen/*` → `lodash`（12 high）、
`vitest` → `@vitest/mocker`（2 moderate）。八個 runtime `dependencies` 一個都唔喺表上。

## 建議

1. Bump `@graphql-codegen/*` 同 `vitest`
2. **剷走 `pages.yml` 嗰個 `--omit=dev` flag** —— 註釋自己講明
   「dropping the flag is the point, not a regression」
3. 同步更新 `CLAUDE.md` §3 嗰行

⚠️ 執票前重新量一次 —— advisory 清單會變，而且 bump 之後可能出新嘅。
如果 bump 完全樹仍然非零，**返嚟問 user gate 策略**，唔好自己揀一個收窄 threshold
（W-034 就係呢個形狀：gate 行為啱、理由書寫闊咗）。

## 驗證方式

- `npm audit`（**全樹**，冇 flag）exit 0
- `pages.yml` 嗰步變返 `npm audit`
- 前端 suite 喺新版 codegen / vitest 之下全綠（bump 可能要改 config）

---

## 2026-09-17 —— 執行紀錄（DevOps，branch `chore/ci/CUI-0049_audit-the-whole-tree`）

### 執票前重新量度（base `77868dd`，`npm ci` 之後）

```
$ cd frontend && npm audit
14 vulnerabilities (2 moderate, 12 high)                 exit 1
$ npm audit --omit=dev
found 0 vulnerabilities                                  exit 0
```

同 QA 嗰個讀數**一致**，冇推翻。`npm audit` 只印 `(2 moderate, 12 high)`，冇印
`0 critical` —— 票上寫嘅 `0 critical` 係 QA 補上去嘅，數字本身啱。

### Bump

| Package | 前 | 後 |
|---|---|---|
| `@graphql-codegen/cli` | `^5.0.7` | `^7.4.1` |
| `@graphql-codegen/client-preset` | `^4.8.3` | `^6.2.0` |
| `vitest` | `^3.2.4` | `^5.0.1` |

`@vitest/mocker` 係 transitive，跟住 `vitest` 去到 `5.0.1`；`lodash` 喺 bump 之後
**完全離開**依賴樹（`npm ls lodash` 零命中）。冇改任何 config：`codegen.ts`、
`vite.config.ts` 嘅 `test` block、`tsconfig.app.json` 嘅 `types: ["vitest/globals", …]`
全部原封不動照樣行得。

⚠️ `vitest@5` 嘅 `engines` 係 `^22.12.0 || ^24.0.0 || >=26.0.0`，而
`frontend/package.json` 嘅 `engines.node` 仍然寫 `>=20`。CI 用 `NODE_VERSION: "22"`，
本地實測 node `v22.22.2`，`npm ci` 零 `EBADENGINE` warning，所以冇改 `engines`
（改佢係另一個決定：會 raise 對貢獻者嘅最低要求，唔屬本票範圍）。

### Codegen 產出**有變**

`frontend/src/gql/graphql.ts` 由 **345 行縮到 69 行**：client-preset 6 唔再產出
schema-wide type（`Maybe` / `InputMaybe` / `Scalars` / `Activity` / `TrackPoint` …），
淨低 operation / fragment type 加 `*Document`。`gql.ts` 逐字無變；`index.ts` 只多咗
一行 `/* eslint-disable */`。

實測影響 **零**：全個 `frontend/src` 只有 `src/data/api/queries.ts:1` 用 `@/gql`，
而且淨係攞個 `graphql` helper，冇人 import 被剷走嗰批 type ⇒ `tsc -b` 零錯。
`ID` input type 由 `string` 闊咗做 `string | number`（input 側闊咗，唔影響現有 call site）。
`TrackQuery.activity.track` 仍然係 `Array<…> | null` —— `CLAUDE.md` §6 嗰條
nullability 陷阱嘅前提冇變，只係行號由 `318` 變咗（已同步更新 `CLAUDE.md`）。

### Gate 改動

`.github/workflows/pages.yml`：`npm audit --omit=dev` → **`npm audit`**（全樹），
step 名由 `Audit runtime dependencies` 改做 `Audit dependencies (whole tree, dev included)`，
註釋重寫，寫明點解由收窄 scope 變成全樹、以及將來遇到「冇 fix 嘅 upstream advisory」
唔准順手加 `--audit-level` / 排除項。`CLAUDE.md` §3 同步。

### 紅綠分辨（scratch，**冇 commit**）

喺 `scratchpad/audit-red/` 造一個 `npm init -y` 嘅樹，`npm i --save-dev lodash@4.17.20`
（即係只經 **devDependencies** 到達嘅 advisory），再用 `bash -e -c '<run body 原文>'` 行：

```
bash -e -c 'npm audit'              → 1 high severity vulnerability   exit 1   ← 新 body 紅
bash -e -c 'npm audit --omit=dev'   → found 0 vulnerabilities         exit 0   ← 舊 body 綠
```

同一棵樹兩個 body 分得開 ⇒ 剷走 flag 係**真係**擴闊咗 gate，唔係改個名。
真樹上新 body `exit 0`。事後 `md5sum -c` 核實 `pages.yml` / `package.json` /
`package-lock.json` / `CLAUDE.md` 四個真檔案全部 `OK`（未被實驗污染）。

### Gate 結果

`npm audit`（全樹，冇 flag）**0 vulnerabilities / exit 0**；lint 0；typecheck 0；
`npm test` **144 passed / 25 files**（同 base 一樣）；`npm run build` 同
`npm run build:static` 都綠；`actionlint` 兩個 workflow exit 0。
Python 側未受影響：531 pytest、ruff check / format、`run365-schema --check` 全綠。

**冇 dispatch 任何 workflow。**
