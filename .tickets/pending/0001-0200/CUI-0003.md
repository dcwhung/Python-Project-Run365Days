# CUI-0003 MAX_QUERY_DEPTH = 5 喺現行 schema 永遠觸發唔到

**狀態**：pending
**類型**：improvement
**優先級**：🟢 Low
**來源**：QA Report 2026-09-14
**建立日期**：2026-09-14 00:52
**建立人**：QA Agent
**負責人**：Backend Developer
**關聯 QA Report**：`.proj-docs/qa/2026-09-14_qa_p0-batch.md`（S4 / S8）
**Code Review 報告**：`.proj-docs/reviews/2026-09-13_review_p0-batch.md`（AU-001）

---

## 問題描述

AU-001 加咗 `QueryDepthLimiter(max_depth=5)` 作為 query cost 防護。實測證實：

- 呢個 acyclic type graph **最深只到 depth 4**
  （`year → personalBests → longest → track|weather → leaf`）
- `QueryDepthLimiter` 要 `max_depth=3` 先開始拒絕嗰條 depth-4 document

即係話 `max_depth=5` 同 `max_depth=4` 對現行 schema 完全無分別，
呢層防護今日**一次都唔會 fire**。

`MAX_QUERY_DEPTH` 個 docstring 本身有講到呢點（「5 leaves one level of headroom」），
所以唔係寫錯，而係兩件實際後果：

1. AU-001 講嘅「depth limit」對今日嘅攻擊面提供零保護；真正擋住 alias flood
   嗰個係 `MaxTokensLimiter`（實測喺 ~1000 token 正確 fire）
2. **冇任何測試可以透過 public schema 行過呢條 branch**。要驗證只可以
   `build_schema(max_depth=<lower>)` 自己整一個 schema —— 現行 185 條測試冇做

## 重現步驟

```
### depth limiter 機制（build_schema 調低 max_depth）
  max_depth=5: d4=pass    d3=pass    d2=pass
  max_depth=4: d4=pass    d3=pass    d2=pass
  max_depth=3: d4=REJECT  d3=pass    d2=pass
  max_depth=2: d4=REJECT  d3=REJECT  d2=pass
  max_depth=1: d4=REJECT  d3=REJECT  d2=REJECT
```

`d4 = {year{personalBests{longest{track{sec}}}}}` —— 呢個 schema 砌得出最深嘅 document。

## 預期結果

一係將 limit 收到真係會 fire 嘅位（`max_depth=4`，即容許今日最深嗰條、拒絕再深一層），
一係喺註釋同 review 記錄講清楚「今日純屬 headroom，唔提供現行保護」，
並且加一條用 `build_schema(max_depth=...)` 嘅測試釘死個 limiter 真係接得上。

## 實際結果

Limit 設喺一個 unreachable 嘅值，無測試覆蓋，`src/api/schema.py` 雖然報 100% coverage
（`build_schema` 條 line 行過），但**行為分支**從來冇被斷言過。

## 環境

- **測試環境**：dev
- **相關版本**：`08c7ea4`

## 影響範圍

- **受影響功能**：GraphQL query cost 防護（AU-001）
- **受影響用戶**：無現行影響 —— token limit 已經擋住實際 abuse 手法
- **風險**：日後有人加一層 nested field，個 limit 會喺無人為意之下由「永不 fire」
  變成「可能 fire」，而冇測試會捉到呢個轉變

## 截圖 / 錯誤訊息

```
### S4  depth limit (MAX_QUERY_DEPTH=5)  —— 全部 pass，冇一條 fire
  depth ~3   HTTP 200  OK
  depth ~4   HTTP 200  OK
  depth ~5   HTTP 200  OK
  depth ~6   HTTP 200  OK   {year{personalBests{longest{track{sec}}}}}

### S5  token limit (MAX_QUERY_TOKENS=1000)  —— 正確 fire
  alias flood x100  (~602 tokens)   OK
  alias flood x200  (~1202 tokens)  REJECTED  Document contains more than 1000 tokens
```

## 子 Tickets（如有拆分）

無 —— 純 backend。

## 建議修復方向

1. 加一條測試：`build_schema(max_depth=3)` + 最深 document → 斷言被拒；
   `build_schema(max_depth=4)` + 同一條 → 斷言通過。咁樣個 limiter 真係接上咗就有證據
2. 決定 `MAX_QUERY_DEPTH` 究竟要 4（貼身）定 5（headroom），並喺 docstring 講明選咗邊個同點解
3. 順帶：`S-012` 已經記低咗 `MAX_TRACK_POINTS` / `MAX_PAGE_SIZE` 冇寫入 SDL description，
   `MAX_QUERY_DEPTH` 同 `MAX_QUERY_TOKENS` 一樣

## 驗證方式

- 新測試喺 `max_depth` 兩邊各斷言一次，並且會喺有人加深 type graph 時失敗
- 185 + N 條測試全綠

## 完成日期

—

---

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-14 00:52 | — | pending | QA Agent | 建立 ticket |
