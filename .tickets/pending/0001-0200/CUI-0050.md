# CUI-0050 四條 list field 嘅 `ARGUMENT_OUT_OF_RANGE` 分唔開係 `limit` 定 `offset`

**狀態**：pending
**類型**：API contract
**優先級**：🟢 Low
**來源**：QA 2026-09-17（W-035 方案 B 嘅承接票）
**建立日期**：2026-09-17
**建立人**：Main Agent（由 QA Agent 發現冇開票）
**負責人**：Backend Developer
**相關**：CUI-0018 (a)、W-035

---

## 問題描述

W-035 揀咗方案 A（改正措辭，承認個窿），方案 B（加 `extensions.argument`）**講咗另飛但冇開票**
—— 只活喺 `reviews/2026-09-17_review_CUI-0018.md` 同 registry 入面。
`.tickets/` 先係 SSoT（`CLAUDE.md` §7），所以補返呢張。

## QA 獨立重現

| document | `path` | `locations` | `code` | `message` |
|---|---|---|---|---|
| `activities(limit:1000, offset:-1)` | `["activities"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `offset must not be negative, got -1` |
| `activities(limit:-5, offset:0)` | `["activities"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `limit must be between 1 and 1000, got -5` |
| `weight(limit:-5, offset:0)` | `["weight"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `limit …` |
| `weight(limit:500, offset:-1)` | `["weight"]` | L1 C3 | `ARGUMENT_OUT_OF_RANGE` | `offset …` |

**`path` / `locations` / `code` 三樣一模一樣**，只有 `message` 唔同。

⇒ Client 照 SDL 自己嘅忠告「Branch on the code rather than on the message」去做，
**係做唔到修正嘅** —— 佢知道有嘢出界，但唔知要改邊個。

`track` 冇呢個問題（`points` 係佢唯一有界 argument，`path` 唯一決定得到）。

## 影響

🟢 Low，**唔 block release**。補救對兩個 argument 都一樣（改個值），而 client 知道自己送過乜。

## 建議

三個 `BoundsError` raise site 加 `extensions.argument`（`"limit"` / `"offset"` / `"points"`）。

⚠️ **唔好加第三第四個 code** —— 會同 CUI-0018 (a) 立嘅原則（code 跟**補救**走，唔跟 raise site 走）
直接打交，而三個情況嘅補救真係一模一樣。

⚠️ 要**重新論證**「`extensions` 入面淨係得 code」呢條線（CUI-0018 (a) 親手劃嘅），
唔好偷偷加。W-013 唔會擋 —— 現有守衛只禁三個 budget counter key。

**執票時機**：等「真係有 client 要 branch」先做，唔好為咗完整性而做。

## 驗證方式

- 四條 list field × `limit` / `offset` 兩頭，各自攞到啱嘅 `extensions.argument`
- `track` 嗰條路照舊
- W-013 守衛測試仍然綠（`extensions` 冇 budget counter）
- SDL description 同步更新，`run365-schema --check` 綠
