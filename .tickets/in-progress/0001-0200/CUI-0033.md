# CUI-0033 `track()` 契約仲有三個未對齊嘅邊界（`undefined` 上界、非整數/超 Int32 錯誤訊息、未知 id）

**狀態**：in-progress（(a)(b)(c) 已做，等 review）
**類型**：bug / contract alignment
**優先級**：🟡 Medium
**來源**：QA Report 2026-09-16（CUI-0025 batch，§2 —— CUI-0025 行為驗證）
**建立日期**：2026-09-16
**建立人**：QA Agent
**負責人**：Frontend Developer
**相關**：CUI-0025（本票係佢嘅 follow-up）、S-061（reviewer Round 2，方案 B）、CUI-0021、AU-001

---

## 摘要

CUI-0025 做到咗佢承諾嘅核心：**兩個 mode 對合法值嘅輸出逐點一致，對非法值嘅拒絕/接受決定亦完全一致**
（QA 實測 1,095 個 activity × points 組合、322,832 個 `sec` 值，零差異）。
本票係剩低嘅**三個邊界**，全部都係 CUI-0025 個 docstring 講嘅
「a client writes one error path rather than one per mode」**仲未完全成立**嘅位。

三個都**唔阻 CUI-0025 結案**（今日零 production 影響，前端唯一 call site
`ActivityView.tsx:34` 傳 `TRACK_POINTS = 600`，合法）。

---

## (a) 🟡 `undefined` 分支唔受 `MAX_TRACK_POINTS` 管，而個 bound 只係兩個常數啱啱相等

CUI-0025 決定 2 保留 static mode「`points` omitted = 回全部」，理由係
「the export caps a track at 600 rows, so "all of it" is bounded by construction」。

**個 600 唔係結構，係 `src/cli/export_data.py:27` `DEFAULT_POINT_LIMIT` 呢個 CLI argument 嘅 default。**

QA 用**真實數據**行咗一次 `run365-export --points 1200`（365 條真 activity，輸出落 scratch，
`data/processed/` 由頭到尾冇被寫過）：

| 量度 | 預設 export（`--points` 600） | `--points 1200` |
|---|---|---|
| 超過 600 行嘅 track | 0 / 365 | **2 / 365** |
| 最長一條（activity `7264441638`） | 600 行 | **1200 行** |

跟住用**真 `createStaticSource`**（唔係手砌 fixture，真 fetcher 讀真 JSON）同
**真 Flask + Strawberry**（`create_app` + `/api/graphql`）各打一次同一個 activity：

| 叫法 | static mode | api mode（`createApiSource`）|
|---|---|---|
| `track(id, 1200)` | ❌ `RangeError: points must be between 1 and 1000, got 1200` | ❌ 同一句 |
| `track(id, 1000)` | ✅ 1000 點 | ✅ 1000 點（逐點一樣）|
| **`track(id)`（omitted）** | ⚠️ **1200 點** | **600 點**（`DEFAULT_TRACK_POINTS`）|

兩個後果：

1. **Mode 分歧原樣返嚟**：1200 vs 600 —— 即係 CUI-0025 想關嗰種分歧，換咗個入口（`--points`）。
2. **個 ceiling 唔係輸出嘅 ceiling**：同一個 function 會**拒絕**「畀我 1200 點」，
   但**照回** 1200 點畀一個冇講數量嘅 caller。`MAX_TRACK_POINTS` 只係「你可以問幾多」嘅上限，
   唔係「你會收到幾多」嘅上限，而 docstring 冇講過呢個分別。

> ⚠️ Reviewer 開 S-061 嗰陣係**推論**呢件事；本票係喺**真實 export 上實測到**，
> 包括「1200 > 1000」呢個 S-061 冇提到嘅第二層。

### 建議方向（由 developer 判斷）

- **方案 A（最平，S-061 方案 A）**：只改 `track()` 註釋，寫明依賴 `--points` 而且 omitted 唔受上界管。
  Trade-off：分歧照喺，下一個人要自己記住。
- **方案 B（S-061 方案 B）**：`undefined` 分支套 `DEFAULT_TRACK_POINTS`，兩個 mode default 一致。
  Trade-off：**第二個 breaking change**，而且用戶已拍板「omitted 回全部」—— 要用戶再拍板先做得。
- **方案 C（QA 傾向）**：`undefined` 維持「回全部」（尊重用戶決定），但喺 `downsample` 之前
  用 `MAX_TRACK_POINTS` 封頂，即 omitted = 「全部，最多 1000 點」。
  咁「個 ceiling 係輸出嘅 ceiling」就成立，而今日（stored ≤ 600）行為**零改變**，唔係 breaking change。
  Trade-off：`--points 1200` 之下兩個 mode 仍然係 1000 vs 600，分歧收窄但未消失。

---

## (b) 🟢 兩個 mode 對同一個非法值講唔同句子

CUI-0025 決定 3 寫明「錯誤形態**逐字一樣**」。QA 逐個值實測（static = 真 `createStaticSource`，
api = 真 Flask + GraphQL）：

| `points` | static mode | api mode | 一致？ |
|---|---|---|---|
| `-1` | `points must be between 1 and 1000, got -1` | 同一句 | ✅ |
| `0` | `... got 0` | 同一句 | ✅ |
| `1001` | `... got 1001` | 同一句 | ✅ |
| `2.5` | `... got 2.5` | `Int cannot represent non-integer value: 2.5` | ❌ |
| `2147483648` | `... got 2147483648` | `Int cannot represent non 32-bit signed integer value: 2147483648` | ❌ |

**拒絕與否完全一致**，只係句子唔同：後兩行喺 api mode 係俾 GraphQL 嘅 `Int!` coercion
喺入 resolver 之前擋咗，`_track_points` 根本冇被叫過。

`checkPoints` 個 docstring 有提「api mode cannot even express one (its SDL argument is `Int!`)」，
所以非整數嗰行算**已披露**；但**超 Int32** 嗰行冇任何文件提過，
而第一句「Rejects the same values and says the same sentence」讀落係覆蓋成個 domain。

### 建議

改 `checkPoints` docstring 一句，講清楚「same sentence」嘅範圍係
「GraphQL `Int!` 表達得到嘅值」，`Int!` 表達唔到嗰啲兩邊都拒絕但句子唔同。純 docstring，零行為改動。

---

## (c) 🟢 未知 id + 非法 `points`：一邊掟錯，一邊靜靜回 null

| 叫法 | static mode | api mode |
|---|---|---|
| `track("no-such-activity", 0)` | ❌ `RangeError: points must be between 1 and 1000, got 0` | ✅ `{"activity": null}`，**零 error** |
| `track("no-such-activity", 600)` | ❌ `ENOENT ... tracks/no-such-activity.json` | ✅ `{"activity": null}`，零 error |

成因唔係 bug：static mode 嘅 bounds check **刻意**喺 fetch 之前
（QA 已核實呢個 docstring claim 成立 —— 壞 id + `points: 0` 出 `RangeError` 而唔係 `ENOENT`，
即係真係一個 request 都冇發），而 api mode 個 `activity(id:)` 回 `null` 之後
`track` resolver 根本唔會行。

今日零影響（前端個 id 一定嚟自 `activities` list）。值得記低係因為佢係
「one error path」呢個賣點嘅最後一個窿。

### 建議

文件化即可（同 (b) 一齊喺 `track()` 註釋講）。**唔建議**為咗對齊而改 api mode 嘅 null 行為。

---

## 驗證方式

- `run365-export --points 1200` 之後，兩個 mode `track(id)`（omitted）嘅長度差距收窄或消失（視乎揀邊個方案）
- 新增測試釘住「omitted 之下最多回幾多點」呢個契約（今日 `source.test.ts:89` 只釘住「回全部」）
- `tests/test_api.py` / `frontend/src/data/static/source.test.ts` 全綠
- `.venv/bin/run365-schema --check frontend/schema.graphql` 綠（如果改動掂到 SDL 文字）

---

## 處理進度（2026-09-16，Backend Developer）

### (b) ✅ 完成

`checkPoints` docstring 改咗，寫明「same sentence」嘅範圍係
「GraphQL `Int` scalar 載得起嘅值」，並附上實測對照表。零行為改動。

api mode 嗰半邊加咗 `tests/test_api.py::test_a_points_the_int_scalar_cannot_carry_is_refused_before_the_resolver`
（param：`2.5` / `2147483648`）。條測試除咗 assert 個 coercion 訊息，仲 assert
**唔包含** `points must be between` —— 冇呢句反向 assertion，一個「其實行到 resolver、
只係措辭古怪」嘅實作一樣會綠。另外 assert `data is None`：coercion failure 唔係 field error，
會 null 成個 response，唔係淨係 null 個 `track`。

### (c) ✅ 完成（附一個張票冇提到嘅發現）

`track()` 註釋寫低咗分歧同埋**點解唔對齊**（靜態嗰邊「非法參數唔使一個 request」
係一個真實性質，唔值得為咗對稱而放棄）。

兩邊各加一條測試：
- `tests/test_api.py::test_an_unknown_activity_swallows_an_illegal_points_that_static_mode_refuses`
- `source.test.ts`「refuses an illegal points before the fetch, even for an id that does not exist」，
  當中 `expect(fetcher).not.toHaveBeenCalled()` 先係帶住個 claim 嗰句 ——
  淨係 assert throw 嘅話，check 行喺 fetch 之後一樣綠。Mutant（將 check 搬去 fetch 之後）實測被殺。

**張票冇記錄嘅一層**：(b) 同 (c) 會互相抵銷。未知 id + `points: 2.5` 喺 api mode
**照樣出錯**（`Int cannot represent non-integer value: 2.5`），因為 coercion 行喺所有 resolver 之前，
根本未有個 null parent 去食佢。即係「未知 id 會食咗非法 points」只喺 `Int!` 載得起嗰啲值先成立。
已由 `test_an_unknown_activity_does_not_swallow_a_points_the_int_scalar_cannot_carry` 釘住。

### (a) ✅ 完成 —— 用戶拍板 **方案 C**

> omitted 仍然意思係「全部」，但喺 downsample 之前先 cap 喺 `MAX_TRACK_POINTS`。

用戶理由：今日 stored ≤ 600，所以**零行為改變、唔係 breaking change**；
而佢令「**上限係輸出嘅上限，唔淨係問法嘅上限**」變成真。

#### 兩個 mode 各自點實施

| Mode | 改動 |
|---|---|
| **api** | `service.tracks()` 由 `if points:` / `else` 兩條路收窄成一條：`sample = min(points, MAX_TRACK_POINTS) if points else MAX_TRACK_POINTS`。原本「讀晒全部行」嗰條單 statement 路**冇咗**，因為 `points` falsy 而家解作 `MAX_TRACK_POINTS` 而唔係「無上限」，嗰條路已經去唔到。`_sample_filter` 對 `total <= points` 嘅 track 本來就原封不動回晒，所以行為同舊路一樣，只係多咗一個 grouped COUNT。 |
| **static** | `source.ts` 嘅 `track()`：`points === undefined` 由 skip downsample 改成 `limit = MAX_TRACK_POINTS`，之後**無條件** `downsample(rows, limit)`。`downsample` 喺 `n <= limit` 回 copy，所以 ≤ 600 行嘅 track 逐點不變。 |

`min()` 連**明確**傳入嘅超界 `points` 都封頂，令「一條 track 最多 `MAX_TRACK_POINTS` 行」
變成**讀行嗰個 function 自己嘅性質**，而唔係靠前面個 `_track_points` 把關。
兩層對超界 ask 嘅反應唔同（邊界**拒絕**、service **封頂**）係故意嘅：只有邊界嗰層有 client 要答。

#### `MAX_TRACK_POINTS` 搬咗屋

由 `src/api/schema.py` 搬去 **`src/api/service.py`**，`schema.py` 改成 re-export
（`MAX_TRACK_POINTS = service.MAX_TRACK_POINTS`，docstring 講埋點解）。
原因：個 cap 要喺 `tracks()`（GraphQL 下面嗰層）生效，而**service 唔可以 import Strawberry**（分層鐵律）。
喺 Python 入面開第二個常數就係 S-062 / S-065 罰過嘅「第四份 copy」，所以係搬唔係抄。
所有現有 `from run365days.api.schema import MAX_TRACK_POINTS` 照用。

**實測 CUI-0034 個 gate 冇被搬穿**：將 `service.py` 個值改成 `1100`，
`run365-schema --check frontend/schema.graphql` 照樣紅（exit 1）——
因為 `TRACK_DESCRIPTION` 嘅 SDL 文字仍然係由呢個常數砌出嚟。

#### 修正咗一句已知係假嘅註釋

`source.test.ts`「still returns the whole stored track when points is omitted」嗰條測試，
原本寫住 *"the export caps a stored track at 600 rows, so 'all of it' is bounded by construction
on this side"*。**呢句係假嘅**（600 係 `run365-export --points` 呢個 CLI argument 嘅 default，
唔係格式嘅結構保證）。改成誠實描述：真正嘅 bound 係 `MAX_TRACK_POINTS`，
600 只係 `--points` 嘅 default 值，`--points 1250` 就會寫 1250 行。

順手改埋同一個檔案入面 CUI-0034 嗰條測試個註釋 —— 佢指住 `src/api/schema.py` 講「改嗰個常數」，
而常數已經搬咗去 `service.py`。

#### 測試

| 測試 | 釘住乜 |
|---|---|
| `test_an_omitted_points_is_capped_at_the_ceiling_the_field_refuses_above` | omitted 回 1000 而唔係 1250 |
| `test_a_points_over_the_ceiling_is_capped_where_the_rows_are_read` | 明確傳 1250 一樣封頂（`min()` 個 mutant 靠呢條死） |
| `test_an_omitted_points_still_returns_every_stored_row_under_the_ceiling` | 批次入面每條 ≤ ceiling 嘅 track 逐條原封不動（零行為改變嘅證據） |
| `source.test.ts`「caps an omitted points at the ceiling it refuses above」 | static mode 同一件事 |

**Fixture**：唔使新砌。`VARIED_TRACK_LENGTHS` 本來已經有 **1250**（當初為 sampler 而加），
所以 `varied_session` 直接有一條 stored > 1000 嘅 track；
加咗 `OVER_CAP_LENGTH = max(VARIED_TRACK_LENGTHS)` / `OVER_CAP_ID` 兩個**推導**出嚟嘅常數，
再喺測試入面 assert `OVER_CAP_LENGTH > MAX_TRACK_POINTS` ——
將來有人縮短 fixture 嘅話會紅，而唔係靜靜變成一條空測試。
Static mode 嗰邊喺 `FILES` 加咗 `tracks/long.json`（1250 行，`sec` = row index）。

⚠️ 三條測試全部**除咗 assert 長度，仲同「明確問 `MAX_TRACK_POINTS`」嘅結果逐點比對**。
淨係 assert 長度嘅話，一個 `rows[:1000]` / `rows.slice(0, 1000)` 嘅實作一樣係 1000 行、一樣綠，
但回嘅係 track 嘅頭段而唔係平均取樣。實測 oracle：capped-omitted 最後一行 `sec` 係 **1249**，
truncation 會係 999。

#### Mutation testing（清 `__pycache__` + 獨立 process + oracle 比對）

| Mutant | 結果 |
|---|---|
| 去 `min(...)`（`sample = points if points else MAX_TRACK_POINTS`） | ☠️ `test_a_points_over_the_ceiling_is_capped_where_the_rows_are_read` |
| `else 10**9`（等同還原舊嘅無上限行為） | ☠️ 兩條 omitted 測試 |
| static 改成 `rows.slice(0, limit)` | ☠️ `caps an omitted points...`（長度綠，逐點比對紅） |
| `service.py` 常數 `1000` → `1100` | ☠️ `run365-schema --check` exit 1 |

#### 殘留（記錄，唔處理）

`--points 1250` 之下兩個 mode 嘅 omitted 仍然係 **1000 vs 600**
（static 讀 1250 行嘅 JSON 封頂 1000；api mode 經 GraphQL 根本冇 omitted 呢個狀態，
`points: Int! = 150` 有 default，client 見到嘅係 `DEFAULT_TRACK_POINTS`）。
呢個殘留係 **`--points` 嘅問題，唔係 `track()` 嘅問題**：兩個 mode 讀緊同一個 export
先至係前提，而 `--points` 容許寫出一個 static bundle 同 SQLite 唔同密度嘅世界。
方案 C 收窄咗分歧（由「無上限 vs 600」變成「1000 vs 600」）但關唔死佢，用戶已知悉。

---

## 完成日期

2026-09-16
