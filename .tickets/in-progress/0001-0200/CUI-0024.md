# CUI-0024 清 AU-035 嗰批 `docs/` 既有 drift —— 寫死嘅數字同已消失嘅 symbol

**狀態**：in-progress
**類型**：tech-debt
**優先級**：🟢 Low
**來源**：CUI-0015 修復過程中由 Backend Developer 申報，Main agent 逐項核實
**建立日期**：2026-09-16
**建立人**：Main agent
**負責人**：Project Manager（lane C）
**相關**：AU-035（記錄咗 `docs/` 既有 drift）、CUI-0015（處理咗**會反覆再發生**嗰類 CI 描述 drift）

---

## 問題描述

CUI-0015 收窄咗 `docs/` 入面描述 CI 行為嘅段落（嗰段喺同一個 session 內過時咗兩次）。
做嗰陣 developer 見到**同一批檔案入面**仲有幾處既有 drift，但按 scope 紀律冇順手修，只喺 receipt 申報。

呢張票收拾嗰批。同 CUI-0015 嘅分別：CUI-0015 處理嘅係**會反覆再發生**嘅 drift；
呢批係**一次性嘅事實錯誤**，改完就啱（除非再引入新嘅寫死數字 —— 見下方「建議方向」）。

## 逐項核實結果

Main agent 已逐項對住代碼核實，以下全部**確認成立**：

| # | 位置 | 文件寫住 | 實際 | 核實方法 |
|---|---|---|---|---|
| 1 | `README.md:118` | 「Installing the package registers **three** console scripts」 | **4 個** | `pyproject.toml` `[project.scripts]`：`run365-activities` / `run365-weather` / `run365-export` / `run365-schema` |
| 2 | `docs/architecture.md:28` | 「**Three** console scripts that wire the features together」 | **4 個** | 同上 |
| 3 | `docs/architecture.md:104` | 「file I/O is limited to `load_jsonl` and **`write_data_js`**」 | `write_data_js` **已唔存在** | `grep -rn write_data_js src/` 零命中；`src/export/static_json.py` 只有 `_dump` / `write_static_json`。`load_jsonl` 仍然存在（`src/dashboard/builder.py:112`），冇錯 |
| 4 | `docs/architecture.md:139` | 「CLI serialiser (**133 tests**)」 | **364**（Round 2 之後）| `pytest tests -q` |
| 5 | `README.md:212` | 「**93 tests** cover the geo and time helpers…」 | **364**（Round 2 之後）| 同上 |
| 6 | `README.md:214` | 「Front end: **87 Vitest** tests」 | **126 / 22 files**（Round 2 之後）| `cd frontend && npx vitest run`，2026-09-16 實測 |

### ⚠️ 核實過**冇錯**、唔好順手改嘅嘢

- `README.md:216` 「all **nine** views」—— `frontend/src/views/` 實際有 9 個 view 目錄
  （activities / activity / load / overview / performance / settings / weather / weight / year）。**正確，唔好改。**
- `docs/CHANGELOG.md:117` 提及 `write_data_js` —— 嗰個係**歷史紀錄**，描述當時存在嘅函數，**正確，唔好改。**

## 影響範圍

- **受影響功能**：無 —— 純文件準確性
- **受影響用戶**：讀 README / architecture 想理解個 codebase 嘅人。第 3 項最誤導：
  叫人去搵一個唔存在嘅函數
- **點解值得修**：本 repo 明顯將文件當契約（AU-047 兩輪之中兩個 finding 都係
  「docstring 寫低嘅數字同實測對唔上」），同一標準應該適用於 `docs/`

## 建議方向

### 逐項修正
第 1–5 項按上表更正；第 6 項先量度再更正。

### ⚠️ 但更新數字唔係最好嘅答案
「93 tests」由啱變錯，係因為**冇任何機制令佢跟住測試數走**。今日改成 364，
下次加測試又會過時 —— 即係 CUI-0015 處理嗰類問題嘅細粒度版本。

所以建議**優先考慮直接移除寫死嘅測試數量**（第 4、5、6 項），改成描述覆蓋咗乜
（「geo 同 time helper、MET 同卡路里計算、體重解析…」呢部分本身係準確而且唔會 drift 嘅），
唔講幾多條。同 CUI-0015 揀方案 3 嘅理由一致：文件唔再持有一份會 drift 嘅 copy。

第 1、2 項（console script 數目）同樣可以考慮寫「the console scripts listed in
`pyproject.toml`」而唔係數字，但呢個數字改動頻率低好多，直接更正亦可接受 —— 執票者判斷。

## 行號會變

CUI-0015（branch `fix/docs/CUI-0015_ci-description-drift`，commit `d88d760`）已經改咗
`docs/architecture.md`、`README.md`、`docs/deployment.md` 嘅 CI 段落。
**執票時以內容定位，唔好照住上表嘅行號去搵。**

## 驗證方式

- 逐項對返代碼 / `pyproject.toml` / 實際測試輸出
- `grep -rn "write_data_js" README.md docs/architecture.md` 零命中（`docs/CHANGELOG.md` 嗰個保留）
- 如果採納「移除寫死數字」嘅方向：`grep -rnE "[0-9]+ tests" README.md docs/` 應該只剩唔會 drift 嘅講法
- 364 條測試全綠（證明冇意外掂到代碼）

> ⚠️ **S-046（Review 2026-09-16 wave 2）**：上表嘅「實際」欄本身就係一個會 drift 嘅數字 —— 由建票
> 到 Round 2 merge 之間，Python 已由 363 → 364、前端由 108 → 126。呢個恰好證明咗下面「移除寫死
> 數字」嘅論點：執票時**唔好**信上表嘅數字，自己量一次。

## 執行紀錄（2026-09-16，lane C）

**自行量度**（base：`d108c82`，即 PR #13 merge 之後嘅當前 head）：

| 量度 | 結果 |
|---|---|
| Python 測試 | **365 passed** |
| 前端測試 | **126 passed / 22 files**（要先 `npm run codegen`，否則 4 個檔案因 `@/gql` 未生成而 fail）|
| Console scripts | **4**（`run365-activities` / `run365-weather` / `run365-export` / `run365-schema`）|
| `frontend/src/views/` | **9** 個目錄 |

⚠️ **呢個數字今日一日之內出現過四個唔同讀數**：

| 讀數 | 出處 |
|---|---|
| 363 / 108 | 建票時 |
| 364 / 126 | Round 2 之後 |
| 351 / 108 | 本 lane 第一次量度，喺 `e827d95`（PR #13 merge **之前**嘅 `develop`）|
| **365 / 126** | 本 lane rebase 落 `d108c82` 之後重新量度，同 Main agent 一致 |

冇一個讀數係「量錯」—— 測試數**取決於你喺邊個 merge state 量**。同一日、同一個 repo、
唔同 branch 就已經係唔同值。**即係話：任何寫死嘅測試數字，喺某啲 branch 上必然係錯嘅。**
呢點喺本 lane 身上實際發生過一次：第一版 commit message 寫低咗「351」，rebase 之後即刻變咗錯，
要一齊改。連記錄呢件事嘅文字本身都 drift 咗 —— 直接否定「更新數字」呢個方向。

**採納方案：移除寫死數字（第 1、2、4、5、6 項），只有第 3 項係事實更正。**

| # | 處理 |
|---|---|
| 1 | `README.md`「three console scripts」→「the console scripts listed below（full set 喺 `pyproject.toml` `[project.scripts]`）」。下面本身就有一個列齊 4 個 script 嘅表，數字係冗餘嘅 |
| 2 | `docs/architecture.md`「Three console scripts」→「The console scripts…（declared under `[project.scripts]`）」 |
| 3 | 刪走 `write_data_js`（已不存在）；保留 `load_jsonl` —— 核實過佢係 `dashboard.builder` 入面**唯一**嘅 file I/O（`src/dashboard/builder.py:112`，唯一一個 `open(` 就喺佢入面），所以「file I/O is limited to `load_jsonl`」改完之後係準確嘅 |
| 4 | 刪走「(133 tests)」 |
| 5 | 「93 tests cover…」→「The Python suite covers…」，覆蓋範圍描述原封不動 |
| 6 | 「87 Vitest tests cover…」→「the Vitest suite covers…」，覆蓋範圍描述原封不動 |

**核實過冇錯、冇改**：
- `README.md`「all **nine** views」—— 實測 `frontend/src/views/` 9 個目錄，正確
- `docs/CHANGELOG.md` 嘅 `write_data_js` —— 歷史紀錄，且喺 lane scope 以外，保留

**Gate**：`pytest tests -q` 365 passed、`ruff check src tests` All checks passed（證明冇掂到代碼）。

## 完成日期

2026-09-16

---

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-16 | — | pending | Main agent | 由 CUI-0015 修復過程申報，逐項核實後建票 |
| 2026-09-16 | pending | in-progress | Project Manager（lane C）| 執票：自行量度後採納「移除寫死數字」方向 |
| 2026-09-16 | in-progress | in-progress | Project Manager（lane C）| Rebase 由 `e827d95` 移去 `d108c82`（PR #13 後），六項 anchor 逐句重新核實，數字重新量度 |
