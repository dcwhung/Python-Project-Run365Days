# CUI-0023 CUI-0004 引嘅 coverage 依據已經過期：`service.py:150` 而家有覆蓋，唯一未覆蓋嘅 line 搬咗去 L257

**狀態**：completed
**類型**：docs / ticket hygiene
**優先級**：🔵 Low
**來源**：QA Report 2026-09-16（low-cost wave 1 batch，§回歸 — Coverage）
**建立日期**：2026-09-16
**建立人**：QA Agent
**負責人**：Backend Developer
**關聯 ticket**：**CUI-0004**（pending，未做）、`2a043f7`（AU-050）、AU-047 C-001 測試

---

## 問題描述

`CUI-0004` 個 title 同 §問題描述 有兩個 claim：

1. `track(points: 1)` 只回最後一點，同 `_even_positions` docstring 嘅
   「first and last row included」相反 —— **仍然成立**；
2. 「**且係全 module 唯一未覆蓋嘅 line**」，票入面引咗
   `src/api/service.py    84    1    99%   150` —— **已經唔成立**。

第 2 點過期嘅原因係 AU-050（`2a043f7`）令 `service.py` 由 84 條 statement 變成 103 條，
行號全部推後；同時 AU-047 嘅 C-001 測試用咗 `CHEAP_TRACK = "track(points: 1)"`
（`tests/test_api.py:750`），**順帶行咗** `points < 2` 嗰條早返 branch。

## 實測（2026-09-16，363 passed）

```
src/api/service.py    103    1    99%   257
```

逐行核實：

| 項目 | CUI-0004 寫嘅 | 2026-09-16 實測 |
|---|---|---|
| module statement 數 | 84 | 103 |
| 未覆蓋 line 數 | 1 | 1（數目啱） |
| 未覆蓋嘅係邊行 | `150`：`return [total - 1]` | **`257`：`return found`** |
| `return [total - 1]` 而家喺 | L150 | **L152，已被覆蓋** |
| 覆蓋佢嘅測試 | — | `test_a_cheap_track_is_refused_once_the_points_budget_is_spent` 等用 `track(points: 1)` 嘅測試 |

新嗰條未覆蓋 line 係 `tracks()` 嘅空輸入護欄：

```python
wanted = list(dict.fromkeys(str(a) for a in activity_ids))
found: dict[str, list[dict]] = {activity_id: [] for activity_id in wanted}
if not wanted:
    return found          # <- L257，唯一未覆蓋
```

呢條由 GraphQL 入唔到（`tracks()` 只喺有 batch 嗰陣先被叫），所以佢係一條
defensive guard 而唔係一個 bug —— 同 CUI-0004 嗰條「文檔講一套做一套」嘅 line
性質完全唔同。

## 影響

**零 production 影響**（純粹係 ticket 內容準確度），但係會令 CUI-0004 被執起嗰陣出事：

- 執票嘅人會照住票去搵 `service.py:150`，睇到嘅係 `_even_positions` docstring 嘅
  `Returns:` 尾段，唔係佢預期嗰行，要重新定位；
- 票上寫「加測試令 line 150 有覆蓋」呢個 DoD **已經自動達成**，
  照做會寫一條重複現有覆蓋嘅測試；
- 真正嘅修復（`points == 1` 要回 first 定 last，同 docstring 對齊）反而會
  被當成已經做咗一半。

`CUI-0004` 嘅**核心 bug 仍然有效**，唔好因為呢張票而 close 佢。實測：

```
_even_positions(total= 600, points=1) = [599]     <- 只有 last，冇 first
_even_positions(total= 600, points=2) = [0, 599]
_even_positions(total= 250, points=3) = [0, 124, 249]
```

## 建議

1. 喺 `CUI-0004` 加一段「2026-09-16 更正」，講明：
   - coverage 依據已過期，`_even_positions` 嗰條早返 branch 而家由 AU-047 C-001 測試覆蓋；
   - title 嘅「且係全 module 唯一未覆蓋嘅 line」要劃走或者改寫；
   - 剩返嘅係純行為問題：`points == 1` 應該回 first 定 last，要同 docstring 對齊。
2. 順便決定 `service.py:257` 要唔要覆蓋（建議唔使 —— 佢係入唔到嘅 guard，
   為佢寫測試係為 coverage 數字寫測試）。

> ⚠️ 呢張票**唔係**叫人改代碼，只係叫人改 `CUI-0004` 嘅內容。
> 真正嘅代碼修復仍然屬於 `CUI-0004`。

## 驗證方式

- `CUI-0004` 檔案入面有 2026-09-16 更正段，且無再聲稱 `service.py:150` 未覆蓋
- `pytest --cov=src` 出到 `src/api/service.py 103 1 99% 257`（或者 257 亦已處理）

---

## 解決方案（2026-09-16）

兩個建議都已滿足：

1. **CUI-0004 已有更正段** —— Round 2 Lane D 執票時已經加咗「⚠️ 先更正一半依據：coverage 部分已經過期」
   一節，明確引用本票；title 尾段「且係全 module 唯一未覆蓋嘅 line」亦已由 main agent 劃走。
2. **`service.py` 嘅 guard 決定唔覆蓋** —— 佢由 GraphQL 入唔到，為佢寫測試等於為 coverage 數字寫測試。

⚠️ **行號第三次改變**：`150` →（AU-050 之後）`257` →（CUI-0019 之後）**`348`**。
2026-09-16 實測 `src/api/service.py  110  1  99%  348`。一個 session 之內郁咗三次 —— 
呢個正好係 CUI-0024 主張「唔好喺文件寫死數字」嘅同一個論點，喺 ticket 層面嘅版本。
**建票時引內容，唔好引行號。**

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-16 | — | pending | QA Agent | Batch QA（low-cost wave 1）核對 CUI-0004 coverage 聲稱時發現已過期 |
| 2026-09-16 13:10 | pending | completed | Main agent | 兩項建議已滿足（CUI-0004 更正段 + title 劃走）；guard 決定唔覆蓋 |
