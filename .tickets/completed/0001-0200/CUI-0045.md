# CUI-0045 `tests/test_api.py` 已經 3,166 行 / 197 個測試，Vercel entry 嘅測試應該拆出去

**狀態**：✅ done
**類型**：tech-debt
**優先級**：🟢 Low
**來源**：Code Review 2026-09-17（CUI-0036 batch，S-099）
**建立日期**：2026-09-17
**建立人**：Main Agent（由 Code Reviewer 發現）
**負責人**：Backend Developer
**相關**：CUI-0035（新加咗 5 條 entry 測試）、AU-001（原有兩條 GraphiQL entry 測試）

---

## 問題描述

`CLAUDE.md` §2 講「一個 feature module 一個檔案」。而 `api/graphql.py`（Vercel entry）
嚴格嚟講唔係 `src/api/` 嘅一部分 —— 佢係 deploy shim。

CUI-0035 加咗 5 條 entry 測試落 `tests/test_api.py`，放喺既有 `_load_vercel_entry`
（AU-001 加嘅）隔籬係合理嘅**局部**決定。但成個檔案而家係 **3,166 行 / 197 個測試**，
已經好難 navigate。

⚠️ Reviewer 明確講咗：**呢個係 pre-existing 問題，唔應該記喺 CUI-0035 嗰條 lane 數上。**

## 建議

抽 `tests/test_vercel_entry.py`，把下列嘢一齊搬過去：

- CUI-0035 嗰 5 條 start-up failure path 測試
- AU-001 嗰兩條 GraphiQL entry 測試
- `_load_vercel_entry` helper
- `VERCEL_ENTRY` / `GRAPHIQL_ENV` 等共用常數

⚠️ 搬之前要確認冇其他測試依賴嗰啲常數（`grep` 全檔）。

## 驗證方式

- 測試總數不變（搬唔係刪）
- `api/graphql.py` coverage 維持 **100%**
- 全 suite 綠、ruff clean


---

## 執行結果（2026-09-17，Backend Developer）

`tests/test_vercel_entry.py` 已抽出。**純機械搬移，一行測試邏輯都冇改。**

### 票上數字漂咗（實測）

| 票寫 | 實測（base `71dad80`） |
|---|---|
| 3,166 行 | **3,543 行** |
| 197 個測試 | **228 個**（parametrize 展開後） |

### 搬走咗乜

| 段落 | 原檔行號 | 內容 |
|---|---|---|
| `VERCEL_ENTRY` 常數 | 51 | 1 行 |
| AU-001 block | 869–896 | banner + `_load_vercel_entry` + **2 條** GraphiQL entry 測試 |
| CUI-0035 block | 3443–3543（至 EOF） | banner + `START_UP_ERROR` + `_entry_with_failing_start_up` + **5 條** start-up failure path 測試 |

合共 **7 條測試**、130 行。測試總數 **531 → 531**（`test_api.py` 228 → 221，新檔 7）。
檔案：`test_api.py` 3,543 → 3,406 行；`test_vercel_entry.py` 153 行。

### grep 確認咗乜（⚠️ 票上有一點唔啱）

- `VERCEL_ENTRY`：只喺 `_load_vercel_entry` 用 → 乾淨搬走。
- `_load_vercel_entry` / `START_UP_ERROR` / `_entry_with_failing_start_up`：只服務呢 7 條 → 乾淨搬走。
- **`GRAPHIQL_ENV` 唔係 Vercel entry 專用**。票寫佢係「共用常數」要一齊搬，但 grep 顯示
  `test_api.py:904/909/914`（W-008 introspection 三條）都用緊佢，而嗰三條測嘅係
  `src/api/schema.py` 經正常 app 嘅行為，**唔應該跟住走**。
  處理：`test_api.py` 嗰句原封不動保留，新檔各自定義同一句一行常數（加註釋交代點解重複）。
  用 cross-module import（`from tests.test_api import GRAPHIQL_ENV`）會令新檔反過來依賴舊檔，
  同拆檔嘅目的相反。

### 搬移純度證明

`git diff -M`（以及 `-C -C` find-copies-harder）**認唔到 rename** —— 預期之內：
`test_api.py` 冇被刪，搬走嗰 137 行只佔原檔 4%，達唔到 similarity 門檻。
改用逐字節核對（一次性 script，對 `HEAD:tests/test_api.py` 同工作區兩個檔案做 byte 比對）：

- 三段 segment 全部以**原封不動嘅 byte** 出現喺新檔（1 / 28 / 101 行，verbatim）。
- `test_api.py` **等於**「原檔減去嗰三段」，唯一額外差異係 `importlib.util` / `json` / `sys`
  三個 import 搬走之後變成 unused（ruff `F401`），照剷。
- 新檔除咗 module docstring、import block 同嗰句 `GRAPHIQL_ENV` 之外，**冇加過任何 `def test`**。

### Mutant 仍然殺得到（同一個 mutant，搬移前後各行一次）

Mutant：`api/graphql.py` 嘅 `_error_app` 入面
`message = f"{type(exc).__name__}: {exc}"` → `message = type(exc).__name__`。
in-process 裝法：把測試 module 嘅 `VERCEL_ENTRY` `setattr` 去一份寫喺 temp dir 嘅 mutated copy
（所以真檔案嘅 `__pycache__` 完全唔會攪入嚟），再**assert 個 mutant 真係生效**
（`/api/health` 回 `{"error": "RuntimeError"}`，reason 冇咗）先至行測試。

| 時機 | 來源檔 | 結果 |
|---|---|---|
| 搬移前 | `tests/test_api.py` | `test_a_failed_start_up_reports_the_reason_instead_of_serving_the_api` **KILLED** |
| 搬移後 | `tests/test_vercel_entry.py` | 同一條測試 **KILLED**，失敗訊息一字不變 |

### Gates

531 passed（不變）、`api/graphql.py` coverage **100% → 100%**（44 stmts / 0 miss）、
TOTAL 96% → 96%、`ruff check` / `ruff format --check` clean、`run365-schema --check` up to date。
改動檔案冇一個喺 `frontend/` 之下，前端 gate 按指示略過。
