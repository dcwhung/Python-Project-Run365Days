# CUI-0013 collect_weather.py 零覆蓋且用 print()，並收拾 collector 遺留嘅小債

**狀態**：✅ done
**類型**：tech-debt
**優先級**：🟢 Low
**來源**：CUI-0010 修復過程中由 Backend Developer 申報
**建立日期**：2026-09-14
**負責人**：Backend Developer

---

## 三項

### 1. `src/cli/collect_weather.py` 覆蓋率 0%，且用 `print()` 而非 logger

CUI-0010 令三個 collector 由 0% 升到 100%，但**佢哋嘅 CLI 入口仍然係 0%**（33 statements）。
呢個亦係 AU-013（零 logging）喺 CLI 層嘅手足。

⚠️ 注意：`src/cli/` 其他檔案嘅 `print()` 係**合理**嘅（CLI 輸出俾用戶睇），
所以呢條唔係「將所有 print 改 logger」。要分清楚「進度輸出」（print 啱）
同「錯誤 / 跳過紀錄」（logger 啱）。

### 2. `_REQUEST_TIMEOUT` 喺三個 module 各有一份

CUI-0010 為咗留喺 lane scope 內，喺 `hko_daily.py` / `hourly.py` / `warnings.py`
各定義咗一次 `_CONNECT_TIMEOUT_SEC = 5` / `_READ_TIMEOUT_SEC = 30`。

應該收歸 `src/common/config.py`（其他 tunable 都喺嗰度）。一行嘢，但要有個 lane 擁有嗰個檔案。

⚠️ CUI-0010 嘅測試係斷言**要求**（有 bounded pair、connect ≤ 10s、read ≤ 60s）而唔係字面值，
所以收歸唔會撞爛測試 —— 呢個係當時特登咁設計嘅。

### 3. 三個 collector 佈滿 magic index 同 slice literal

`data[2]` / `data[11]`、`tds[9]`、以及 `[:-2]` / `[:-5]` / `[:-1]` 等 slice。

CUI-0010 特登冇動，理由充分：命名佢哋係一次跨越三張票都冇掂嘅行數嘅可讀性重構，
分開做 diff 更易 review。**而家安全咗** —— 解析出嚟嘅值已經被 100% 覆蓋嘅測試釘死。

## 驗證方式

- `collect_weather.py` 覆蓋率 ≥ 80%
- `grep -rn "_CONNECT_TIMEOUT_SEC\|_READ_TIMEOUT_SEC" src/weather/collectors/` 零結果（已收歸 config）
- 現有測試全綠，且 timeout 測試唔使改
- 365 個真實檔案 export 後 static JSON 仍然 byte-identical

---

## 狀態歷史

| 日期 | 舊狀態 | 新狀態 | 負責人 | 原因 |
|------|--------|--------|--------|------|
| 2026-09-14 | — | pending | Main agent | 由 CUI-0010 修復過程發現 |
| 2026-09-17 | pending | ✅ done | Backend Developer | 三項全部處理，CLI 覆蓋率 45% → 98% |

---

## 修復紀錄（2026-09-17）

**Branch**：`fix/weather/CUI-0012_guard-the-collector-parsing` ｜ **Base**：`6576e56`

### 三項逐項

1. **`collect_weather.py` 覆蓋 + logger**：拆出可測嘅 `run()`（跟 `process_activities.py` 嘅
   形狀）同 `_collect_source()`。進度照用 `print()`（用戶要睇嘅 running commentary），
   **失敗改用 `logger.warning`** —— 失敗係「冇做到乜」嘅紀錄，應該同 collector 自己嘅
   skip warning 同一條 stream。順手修好 per-source 隔離：一個 source 死唔再連累另外兩個，
   但 exit code 仍然非零。新增 `tests/test_cli_collect_weather.py`（14 條）。
   **覆蓋率 45% → 98%**（唯一未覆蓋係 `if __name__ == "__main__"`）。

2. **`_REQUEST_TIMEOUT` 收歸**：三份重複 → `src/common/config.py` 嘅
   `HTTP_CONNECT_TIMEOUT_SEC` / `HTTP_READ_TIMEOUT_SEC` / `HTTP_REQUEST_TIMEOUT`。
   票講得啱：CUI-0010 嘅 timeout 測試斷言**要求**而唔係字面值，所以一條都唔使改。

3. **Magic index / slice literal 命名**：三個 collector 嘅 column index 全部改成具名常數；
   `[:-2]` / `[:-5]` / `[:-1]` 改用 `removesuffix()` 按**名**除單位 —— 除咗可讀，
   仲順手修好一個弱點：單位唔見嗰陣舊寫法會斬走真數字，`removesuffix` 唔會。

### 票上講法嘅更正（實測）

1. **「覆蓋率 0%」已經過時**：喺 base `6576e56` 實測係 **45%**（33 statements，18 未覆蓋）。
   開票嗰陣應該真係 0%，但 CUI-0011 加嘅 `TestCollectThenExport` 已經開始
   `from run365days.cli.collect_weather import _write_jsonl`，順手覆蓋咗一部分。
   「零覆蓋」呢個講法唔準確，**未覆蓋嘅係決策邏輯**（source 選擇、失敗處理），呢個先係重點。

2. **驗證方式第 4 項做唔到**：「365 個真實檔案 export 後 static JSON 仍然 byte-identical」——
   `data/processed/` 係 git-ignored，本 lane 冇真實資料，驗證唔到。
   間接證據：440 條測試全綠，包括 CUI-0011 嗰批 collect → write → read → export round-trip。

### Mutation 證據

三個 mutant，同樣 in-process `setattr` + 先 assert 生效 + 獨立 oracle：

| Mutant | 結果 |
|---|---|
| `_collect_source` 唔再 catch（一個死 source 拖冧成個 run） | KILLED |
| 失敗改用 `print()` 而唔係 logger | KILLED |
| `run()` 硬回 0（exit code 永遠成功） | KILLED |
