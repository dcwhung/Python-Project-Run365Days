"""Flask + Strawberry GraphQL API over the exported SQLite database.

Create the application with :func:`run365days.api.app.create_app`; the
schema lives in :mod:`run365days.api.schema` and every database access in
:mod:`run365days.api.service`.
"""
